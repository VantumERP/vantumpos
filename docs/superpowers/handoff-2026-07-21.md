# VantumPOS — Session Handoff (2026-07-21)

This doc lets a fresh Claude Code session pick up the Serbian-law-alignment work exactly where it left off. **Copy the fenced prompt below into the new session**; the sections after it are reference context the prompt points at.

---

## ▶ Handoff prompt (copy everything in this block)

```
You are continuing the Serbian-law-alignment effort for VantumPOS, a local-first
Tauri v2 (Rust + rusqlite/SQLite) + React/TypeScript/shadcn/bun point-of-sale app
for a Serbian retail pilot. Working dir: /Users/adnan/Projects/vantumpos.

READ FIRST (in this order), then confirm you understand the state before acting:
1. docs/superpowers/handoff-2026-07-21.md      ← this handoff (full context)
2. docs/SERBIAN-LAW-COMPLIANCE.md              ← the master compliance report + roadmap
3. docs/KEP-VERIFIED-RULES.md                  ← verified KEP law (if touching the KEP)
4. The most recent per-feature spec/plan under docs/superpowers/specs|plans/

WHERE WE ARE:
- The KEP (Knjiga evidencije prometa) is COMPLETE: SW-9a (value ledger), SW-9b
  (kalkulacija / storno / nivelacija / corrections), SW-9c (year-end close). All
  merged to master locally.
- master is ~145 commits AHEAD of origin/master and UNPUSHED. There is a stale
  leftover branch feat/kep-evidencija (ignore/clean up only if asked).

STANDING CONSTRAINTS (do not violate — they carry over from the whole effort):
- NEVER boot the app without explicit permission: no `tauri dev`, no dev/preview
  server, no built binary. Verify ONLY via the gate commands below.
- NEVER push master to origin without an explicit "push" instruction from the user.
- Commit trailer on EVERY commit (repo convention across the whole series):
      Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
- Money = integer minor units (para); quantities = milli-units; timestamps =
  RFC3339 passed in as a `now: &str` param — never datetime('now') in ledger/
  closure code. Ledger is append-only (never UPDATE/DELETE a posted kep row).
- Serbian Latin diacritics (č, ć, š, ž, đ) in all user-facing copy.
- Use the Opus model and the project skills (tauri-v2, shadcn, rust-best-practices,
  react best-practices) per the user's memory.

THE COMPLIANCE-CYCLE MACHINE (follow it for each new feature — it has caught a
real legal error in EVERY module, so do not skip the verification passes):
1. Verify the primary law text with an adversarial Workflow BEFORE encoding it;
   correct docs/SERBIAN-LAW-COMPLIANCE.md (or the relevant VERIFIED-RULES doc).
2. superpowers:brainstorming → write + commit a design spec under
   docs/superpowers/specs/YYYY-MM-DD-<feature>-design.md. Get user approval.
3. superpowers:writing-plans → write + commit a TDD plan under
   docs/superpowers/plans/YYYY-MM-DD-<feature>.md.
4. Run a sequential Workflow: per task implement (TDD) → adversarial review → fix,
   then a whole-diff final adversarial review that re-checks against the verified
   law and RE-RUNS ALL GATES. Fix any confirmed legal-format / correctness catch.
5. Independently re-run all 6 gates yourself (below) — trust nothing self-reported.
6. Present results; MERGE TO MASTER LOCALLY only when the user explicitly says so.

GATES (all six must pass; run from repo root):
- bun run test
- bun run build
- cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1
- cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings
- cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
- git diff --check

WHAT TO DO NEXT: Ask me which remaining P1 item to take (see the backlog in the
handoff doc), then run the compliance-cycle machine for it. Do NOT start coding
before we've agreed on scope via brainstorming + an approved spec.
```

---

## Reference context

### Current repo state
- **Branch:** `master`. SW-9c merged fast-forward; latest commit `d9a3115`.
- **master is ~145 commits ahead of `origin/master`, unpushed.** Do not push without an explicit instruction.
- Stale leftover branch: `feat/kep-evidencija` (from an earlier cycle; already merged — clean up only if asked).
- Latest migration: **v14** (`kep_closures`); the `migration_count` assertion in `src-tauri/src/db/mod.rs` is **14**.
- Test counts at handoff: **cargo 349 passed**, **bun 211 passed**; clippy/fmt/build/`git diff --check` all clean.

### What's done
- **P0 compliance** (fiscalization posture, go-live reset, importer encoding, oversell, diacritics, etc.).
- **P1 so far:**
  - SW-6a/6b/6c — campaign engine + campaign evidence + price-history/HTML renderer (čl. 36/37 sniženja).
  - SW-8 — printing stack (export-then-open HTML documents; non-fiscal).
  - SW-7 — reklamacije (consumer complaints; OLD/NEW regime deadline engine).
  - **SW-9 (KEP) — COMPLETE:**
    - **9a** `src-tauri/src/kep.rs` — 5-column value ledger, derived saldo, daily razduženje, receipt zaduženje, corrections.
    - **9b** `src-tauri/src/kep_kalkulacija.rs` + `kep_storno.rs` — 14-element kalkulacija, cause→{kolona,sign,kind} storno map, nivelacija.
    - **9c** `src-tauri/src/kep_close.rs` — year-end zaključivanje: `kep_closures` (migration v14), computed carry-forward (čl. 17 st. 2), irreversible typed-confirm `close_year` + `ensure_year_open` gate wired into all 6 posting paths (čl. 18 prevention), signable close doc (čl. 17 st. 4), statutory 5-column paginated book (DONOS/SVEGA cumulative kolona-4/5, derived krajnji saldo), 5-year retention floor (§5.6), admin-gated commands, KEP-module UI.
  - Command layer: `src-tauri/src/commands/kep.rs`. Frontend: `src/app/kep/KepModule.tsx`, `src/services/{types,ports,local-adapter,mock-adapter}.ts`.

### Remaining P1 backlog (pick one next)
- **Work-time records** (evidencija radnog vremena zaposlenih).
- **Cash / AML trio** (blagajnički limit, dnevni pazar / polog, gotovinski prag).
- **SW-10 audit log** (tamper-evident action log).
- **Cenovnik export** (price-list / declaration output).
- **Employee lifecycle** (hire/terminate, roles, obaveštenja).
- **SW-16** full per-article popis session (P2).
- Deferred KEP follow-ups (each earns its own cycle): **campaign→KEP auto-nivelacija**, **PDV-rate revaluation**.

### Founder decisions pending (not code — the user handles these)
- Push `master` to origin when ready.
- SW-8 live print check on a real receipt/A4 printer (user's machine).
- Submit the PURS pitanje (`docs/compliance/pitanje-purs.md`) and get the DPA / ugovor-o-obradi lawyer review (`docs/compliance/ugovor-o-obradi-nacrt.md`, `evidencija-obrade-cl47.md`).

### Key documents
- **Master report:** `docs/SERBIAN-LAW-COMPLIANCE.md` — compliance status + the P0/P1/P2 roadmap.
- **Verified-law memos** (primary text pinned before encoding): `docs/KEP-VERIFIED-RULES.md`, `docs/ZOT-36-37-VERIFIED-RULES.md`, `docs/ZZP-REKLAMACIJE-VERIFIED-RULES.md`.
- **Compliance drafts:** `docs/compliance/` (PURS pitanje, DPA ugovor nacrt, čl. 47 evidencija obrade, breach runbook, onboarding checklist, employee notice, fiscalization memo).
- **Per-feature specs/plans:** `docs/superpowers/specs/` and `docs/superpowers/plans/` (dated).
- **Progress log:** `docs/PROGRESS.md`; competitive context: `docs/COMPETITIVE-GAP-ANALYSIS.md`.

### Hard-won lessons (why the verification passes are non-negotiable)
Verification-first has caught a real legal error in every KEP module. Most recent (SW-9c): the full-book print had drifted to a **6-column layout with a saldo column**; the primary text (`KEP-VERIFIED-RULES.md` §2.1/§2.7, PEP čl. 15 st. 1) fixes the obrazac at **5 columns with no saldo column** — DONOS/SVEGA carry cumulative kolona-4/5 totals and the krajnji saldo is a *derived line*. The final adversarial pass (re-reading primary law, not just re-running tests) is what caught it. Always run it.
