# SW-16 — Popis — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the annual and nivelacija popis as a two-phase blind count with two statutory signature events, the required separate lists, a structured izveštaj, a computed deadline engine, and a write-lock on posting.

**Architecture:** A state machine (`draft → counting → counted_signed → computed → computed_signed → posted`) is the whole module. Phase B data is unreachable from Phase A **at the query layer**, not merely hidden in the UI — that is what makes PoP čl. 8 st. 5 a property of the system rather than a UI convention. Deadlines are computed from the FS filing rule, never tabulated.

**Tech Stack:** Rust (rusqlite, serde) + Tauri v2 commands; React 19 + TypeScript + shadcn/ui + vitest; bun.

## Global Constraints

- **Money is integer minor units; quantities milli-units.** Never floating point.
- **Timestamps RFC3339 passed as a `now: &str` param.** Never `datetime('now')` in decision code, and **never a wall clock in the deadline engine**.
- **Serbian Latin diacritics** in every operator string; Serbian quotes open `„` and close `“`.
- **Migrations APPEND-ONLY.** Read the current count from `db/mod.rs` first; this is the next free version.
- **NO fine figure outside `legal.rs`**; new notices go into **both** guard lists with the count bumped.
- **No book quantity may be readable during Phase A** — enforced in the query layer and asserted by test.
- **There is no obrazac for the popisna lista.** Do **not** carry over the SW-9c KEP print-fidelity constraints. **But the izveštaj content IS prescribed** (PoP čl. 13 st. 1).
- **Never boot the app.** Verify only via the gates.
- Commit trailer: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

**Gates:** the standard six.

**Governing law:** [REMAINING-SW-VERIFIED-RULES.md](../../REMAINING-SW-VERIFIED-RULES.md) §2c, §3 V5, §4 reqs. 29–42. Design: [2026-08-01-sw16-popis-design.md](../specs/2026-08-01-sw16-popis-design.md).

---

## File structure

**New Rust:** `src-tauri/src/popis.rs` (state machine + deadline engine, pure), `src-tauri/src/commands/popis.rs`.
**Modified Rust:** `db/migrations.rs`, `db/mod.rs`, `legal.rs`, `retention.rs`, `kep.rs` (nivelacija hook), `lib.rs`.
**New frontend:** `src/app/popis/PopisModule.tsx`, `src/app/popis/CountSheet.tsx`, `src/app/popis/IzvestajPanel.tsx` (+ tests).
**Modified frontend:** `services/*`, `app/navigation.ts`.

---

### Task 1: Migration — the popis stores

`popis_sessions`: `id`, `vrsta` (`godisnji | nivelacioni`), `prodajno_mesto`, `datum_popisa`, `period_from`, `period_to`, `status` (the six-state enum), `plan_rada_json`, `odluka_ref`, `usklađivanje_potvrdjeno_at` (the čl. 20 st. 3 gate), `perpetual_odluka_ref`, `posted_at`, `created_at`, `updated_at`.

`popis_lines`: `id`, `session_id`, `lista_vrsta` (`roba | oštećena | van_objekta | gotovina | potraživanja | konsignacija`), `sifra`, `naziv`, `vrsta`, `jedinica_mere`, `stvarna_kolicina_milli`, `blizi_opis`, **`knjigovodstvena_kolicina_milli` (nullable, written only at the Phase A→B transition)**, `cena_minor`, `created_at`, `updated_at`.

`popis_signatures`: `id`, `session_id`, `faza` (`a | b`), `potpisnik`, `potpisano_at`, `snapshot_hash`, `created_at`. Immutable.

`popis_commission`: `id`, `session_id`, `ime`, `uloga`, `rukuje_imovinom` (0/1), `created_at`.

- [x] **Step 1: Write the failing tests** — the tables exist; the status enum is a closed CHECK; a `popis_signatures` row has no `updated_at`; `knjigovodstvena_kolicina_milli` is nullable; **a survival test applying `MIGRATIONS[..N]` to a raw Connection, seeding, `drop(conn)`, then `Db::new`** — mirror commit `270796c`.
- [x] **Steps 2–5.** Landed as migration **v20** (`popis_stores_blind_count_and_posting_lock`). Beyond the four tables, the migration enforces two module invariants in the engine rather than leaving them to a later layer, because migrations are append-only and these two cannot be added later without a second migration the plan does not budget for: **the čl. 8 st. 5 blind count is a write guard** (a book quantity is refused while the session is in `draft`/`counting` **and while no `faza = 'a'` signature exists for it**, so during a count none exists for any query, export or backup to leak — req. 29), and **posting write-locks the session, its lines, its commission and any new signature** (req. 41), with `popis_signatures` immutable from the moment each signature is taken. The guard is anchored on the potpis rather than on `status` alone because `status` is a claim any `UPDATE` can make — a session flipped or born into `counted_signed` with no signature behind it is refused the book data. **Contract for Task 4:** `popis_sign_phase_a` must write the signature row *before* it populates any `knjigovodstvena_kolicina_milli` (the statutory order anyway), and `popis_post` must write everything else *before* flipping to `posted`, since that flip is the last permitted write. No popis table may be written with `INSERT OR REPLACE`/upsert: `REPLACE` deletes and re-inserts, so no `BEFORE UPDATE` lock fires. `DELETE` stays open on all four tables for the req. 42 retention purge, as v18 and v19 each reasoned. Deviations: identifiers and stored enum values are ASCII-folded per the schema-wide convention (`uskladjivanje_potvrdjeno_at`, `ostecena`, `potrazivanja`), and `popis_commission.uloga` is a closed enum (`predsednik | clan | jedno_lice`) rather than free text, so the čl. 6 st. 1 single-person popis is expressible instead of implied.

---

### Task 2: `popis.rs` — the state machine + the blind-count property

**Interfaces:** `PopisStatus` enum, `advance(current, event) -> Result<PopisStatus, AppError>`, `book_quantities_released(status, phase_a_signed) -> bool`. **`book_quantities_released` is the released interface** — this line originally named `book_quantities_visible`, which is only the *status* limb of čl. 8 st. 5 and is now module-private precisely so it cannot be reached; see the Task 2 notes below.

- [x] **Step 1: Write the failing tests**

```rust
    /// PoP čl. 8 st. 5: book quantities must not reach the commission before the
    /// counted state is written and signed. A UI that merely hides them is not
    /// enough — the property belongs to the state machine.
    #[test]
    fn book_quantities_are_invisible_until_phase_a_is_signed() {
        for status in [PopisStatus::Draft, PopisStatus::Counting] {
            assert!(!book_quantities_visible(status), "{status:?} must be blind");
        }
        for status in [PopisStatus::CountedSigned, PopisStatus::Computed, PopisStatus::ComputedSigned, PopisStatus::Posted] {
            assert!(book_quantities_visible(status), "{status:?} may reveal");
        }
    }

    #[test]
    fn phase_b_cannot_be_entered_without_the_phase_a_signature() {
        let error = advance(PopisStatus::Counting, PopisEvent::Compute)
            .expect_err("computing before the čl. 8 st. 5 signature must be refused");
        assert!(matches!(error, AppError::Business { code: "popis_not_signed", .. }));
    }

    #[test]
    fn a_posted_popis_admits_no_further_transition() {
        for event in [PopisEvent::Count, PopisEvent::Compute, PopisEvent::SignA, PopisEvent::SignB, PopisEvent::Post] {
            assert!(advance(PopisStatus::Posted, event).is_err(), "{event:?} after posting");
        }
    }
```

- [x] **Steps 2–5.** Landed as `src-tauri/src/popis.rs`. The plan's three tests compiled and expressed their intent as written, so **no deviation from the plan's code**. Six more were added, each a named behaviour with a real assertion and each verified load-bearing by mutation: the bylaw sequence walked end to end; **all 30 (status, event) pairs swept exhaustively**, asserting the error *code* and not merely `is_err` — the five arrows pass, the other 25 are refused, and signing B before A, re-counting a counted popis and posting an unsigned one are covered by that sweep rather than left to the shop; the two-limb release property; two tests against a real migrated database; and the IPC wire form of both enums, since serde derives it from the *variant identifier* while `as_db_str` is a hand-written literal — renaming `CountedSigned` sends `signed_count` over IPC while the column keeps `counted_signed`, and the schema test alone does not notice (verified: that rename fails only the added assertion). **The blind-count property is split into its two limbs on purpose:** `book_quantities_visible(status)` is the *status* limb, false in `draft`/`counting` — exactly the vocabulary the v20 `popis_lines` triggers refuse a book quantity in — and `book_quantities_released(status, phase_a_signed)` is the whole čl. 8 st. 5 condition and the one a query layer must consult, since `status` is a claim any UPDATE can make and a session can be born in `counted_signed`. The status limb is **module-private**, so „call the released one“ is enforced by the compiler rather than by a doc comment. **Contract for Tasks 4 and 9:** the query layer calls `book_quantities_released`, and when it is false the response carries **no book quantity from any source** — the predicate is not scoped to `popis_lines.knjigovodstvena_kolicina_milli`. The v20 write guard protects that column alone, so it is not the boundary: the perpetual book stock lives in `inventory_balances.quantity_milli` and is derivable from `inventory_movements`, both readable by any query at any time with no trigger in the way. A `popis_lines_list` or a Task 9 count sheet that joins either one to show an „expected“ column during `counting` breaches čl. 8 st. 5 with the whole suite still green. Čl. 8 st. 5 is about book data reaching the commission, whatever table it is read out of. Additions beyond the plan's named interfaces: `book_quantities_released`, `as_db_str`/`from_db_str` (asserted equal to the v20 status CHECK read back off `sqlite_master`, so the enum cannot drift from the engine), and `naziv()` on both enums behind the operator-facing refusal text. Error codes: `popis_not_signed` (the plan's own), `popis_proknjizen` (posted, worded exactly as the v20 posting lock) and `popis_nedozvoljen_prelaz` (everything else).

---

### Task 3: The deadline engine (req. 38)

`izvestaj_due(vrsta, datum_popisa, fs_filing_deadline) -> String`. **Compute `filing − 60 days` for the annual mode; `datum_popisa + 30 days` for nivelacija. Do not hardcode dates.**

- [x] **Step 1: Write the failing tests** — FY2026 → 30 January 2027; FY2027 → 31 January 2028 (**leap year — this is the case a hardcoded table gets wrong**); FY2028 → 30 January 2029; nivelacija → +30 days from the count; the function takes the filing deadline as a parameter so a statutory change is a config edit, not a code change.
- [x] **Steps 2–5.** Landed in `src-tauri/src/popis.rs` alongside the state machine. Twelve tests — nine at first, three more from the review fixes below — each verified load-bearing by mutation: hardcoding the annual answer as „30 January of the filing year“ fails **only** the FY2027 row (`left: 2028-01-30, right: 2028-01-31`), which is exactly the case the plan named. Beyond the plan's four named behaviours: the annual rok does not move when the count date does (čl. 13 st. 2 anchors it on the rok za dostavljanje, and an engine anchored on `datum_popisa` would agree with the FY tests every time the popis fell on 31 December — which for the annual popis is nearly always); the nivelacija limb does not read the filing deadline at all (the dangerous fall-through direction: it would give an in-year popis a rok months later than the 30 days čl. 13 st. 2 allows, and the shop would file late believing the app); the nivelacija cases cross a month end, a year end and a 29 February, so the 30 are calendar days and not „a month“ — the same 5 February is 6 March in 2028 and 7 March in 2027; every unreadable date is refused rather than repaired, including the two near-misses a lenient parser eats (`2027-3-31` and an RFC3339 stamp); and `PopisVrsta` is pinned to the v20 `vrsta` CHECK and to its own IPC wire form, for the reason the status test gives — here the drift would silently swap two **different statutory roks**. **One test asserts the source, not an answer:** design §7 t. 5 („no hardcoded deadline dates“) and the standing „never a wall clock in the deadline engine“ are properties no behavioural test can see — it cannot tell a computed 30 January 2027 from a hardcoded one, nor a rok derived from its arguments from one that read the clock and happened to agree today — so the non-test half of the module is scanned for a `gggg-MM-dd` literal and for every spelling of a wall-clock read: `utc_now`/`now_utc`/`Utc::now`/`Local::now`/`now_local`/`SystemTime::now`/`datetime('now')` (both limbs verified by mutation). **Review fix:** the first of those was missing. `crate::clock::utc_now` is this repository's own canonical reader (47 call sites) and is **not** matched by the substring `now_utc` — neither string contains the other — so the guard was hollow against the one way a clock would actually arrive here; injecting `let _clock = crate::clock::utc_now();` into `izvestaj_due` left all 18 tests green. A second test now pins the scanned list to what `src/clock.rs` actually exports, so a future `pub fn` there fails until the engine's guard has learned to scan for it (both verified by mutation). **Deviations from the plan's stated interface, both about what the function does when it cannot answer:** (1) it returns `Result<String, AppError>` rather than `String` — it parses two dates it does not own (one typed by the shop, one from configuration), and with a bare `String` an unreadable one has nowhere to go but a panic or an invented rok; a rok this module invents is one the owner files on, and the čl. 58 prekršaj attaches to the popis. Codes: `popis_rok_neispravan_datum`, `popis_rok_predaje_fi_nepoznat`, `popis_rok_van_kalendara`. **Review fix:** the third code had no test and only half a refusal. The overflow is genuinely reachable (a nivelacija counted on `9999-12-31`) and was already refused; the underflow was not — `{:04}` renders year −1 as `-001`, so a rok computed back from a year-0000 filing deadline came back as `Ok("-001-12-03")`, a string neither `gggg-MM-dd` nor storable in any v20 date column, returned as a **success**. `iso_datum` now yields `Option<String>` and refuses anything before year 1 through that same code, and a test asserts both directions. (2) `fs_filing_deadline` is `Option<&str>` rather than `&str` — čl. 13 st. 2's second limb does not read a filing deadline, so requiring one would make every nivelacija caller fabricate a date that is then ignored; the annual limb refuses `None` by name instead. Addition: `PopisVrsta` (`godisnji | nivelacioni`), because Task 3's own signature names a `vrsta` and no enum existed; modelled on `PopisStatus`, ASCII-folded like the column. **Contract for Tasks 6 and 7:** čl. 14 st. 2 gives the odluka o usvajanju the rok *„iz člana 13. став 2“* — it is the same date, so it gets no second function and no second deadline field; the izveštaj and the odluka are surfaced from this one answer (req. 38). Task 7 calls it with `PopisVrsta::Nivelacioni` and `None`. Nothing here reads a clock or a setting: the filing deadline is a parameter because ZoRač čl. 44 st. 1 sets 31 March *„osim ako posebnim zakonom nije drukčije uređeno“*, so the rok is not this module's to own — but that also means **no caller yet supplies it**, and Task 4 or 6 owes it a configured source rather than a constant.

---

### Task 4: Commands — sessions, lines, the two signatures, posting

Admin-gated. `popis_open` refuses without the **čl. 20 st. 3 reconciliation confirmation** (req. 39). `popis_sign_phase_a` freezes an immutable snapshot and only then populates `knjigovodstvena_kolicina_milli`. `popis_post` write-locks everything (req. 41).

- [x] **Step 1: Write the failing tests** — opening without the reconciliation flag is refused; a Phase A line write that carries a book quantity is **refused at the boundary**; signing A populates the book quantities in one transaction; posting write-locks; a correction after posting requires a **new session** rather than an edit.
- [x] **Steps 2–5.** Landed as `src-tauri/src/commands/popis.rs` (18 tests) plus one five-line extraction in `popis.rs`. Every named behaviour has a real assertion and **each was verified load-bearing by mutation** — fourteen mutations run, each caught, and the two that were *not* caught are recorded below rather than papered over. The čl. 8 st. 5 property is enforced in **two** places for the two different things it forbids: the write side refuses a payload carrying a book quantity with `popis_knjigovodstvo_pre_potpisa` (refused, never dropped — a client that silently had the field discarded would believe it had stored book data), and the read side takes Task 2's contract literally. `read_lines` is **two statements rather than one and a filter**: while the data is withheld the query does not name `knjigovodstvena_kolicina_milli` and does not name `inventory_balances` or `inventory_movements` either, so there is no value in the row for a refactor, a log line or a debug print to spill. The test asserts it on the **whole serialized response** — a product is seeded with a distinctive perpetual balance and that number must appear nowhere while the popis is counting, and must appear once the potpis is taken, so the first half cannot pass vacuously. Mutation-checked with the exact defect Task 2 warned about: adding an „očekivano“ column to the blind query by joining `inventory_balances` fails **only** that test, with the rest of the suite green. The signature step writes in the statutory order — potpis, then state, then book quantities — and the order is behaviourally proven rather than commented: reversing it fails five tests, because the v20 guard aborts a book quantity written before its potpis. **Deviations and additions.** (1) `popis.rs` gained `posting_lock(status)`, extracted from `advance`'s existing `Posted` early-return with no behaviour change, because the command layer refuses edits that are not transitions at all — a line write, a komisija row — and a second hand-written copy of that sentence would have been free to drift from the engine's and from the v20 trigger's. (2) Beyond the plan's three named commands the module ships `popis_list`, `popis_get`, `popis_save_line`, `popis_start_count`, `popis_compute` and `popis_sign_phase_b`: the machine has five arrows, and Task 2's contract puts the čl. 8 st. 5 enforcement in the **query layer**, which has to exist to be enforced. (3) The komisija is recorded at open with the req. 40 warning, which Task 4 does not name — but no other task creates it, `popis_commission` would be unreachable schema, and Task 9's req. 40 assertion would have nothing to render. The warning names the person, cites čl. 5 st. 1, says the čl. 6 st. 1–2 shodna primena **is not settled** (§6 R-5) and says the popis was not stopped; the test asserts all four, because a warning that read like a refusal would be an operator string promising behaviour this module does not implement. (4) Req. 30's two snapshots are enforced as write rules, not only as hashes: the counted state is frozen by the čl. 8 st. 5 potpis (`popis_stvarno_stanje_potpisano`) and the liste by the čl. 9 st. 3 one (`popis_liste_potpisane`), and in `computed` a line may take its obračun fields but not move the količina the commission signed. Without this a signed potpis could attest to a state that no longer existed. (5) The book quantity is read from `inventory_balances` joined on `products.sku = popis_lines.sifra`, at the moment the potpis is taken — the earliest moment čl. 8 st. 5 permits reading it at all. A šifra that resolves to nothing keeps its NULL rather than a zero: the gotovina and potraživanja liste have no perpetual record behind them and a fabricated zero would report a manjak the shop does not have. (6) **Two mutations were NOT caught and both are stated in the test file.** Dropping the transaction from the signature step passes every behavioural test, because all three writes are stamped with the same `now` and guarded by the same `<> ''` CHECK, so the earliest always fails first and no reachable input leaves a durable half to observe; „in one transaction“ is therefore asserted **structurally** — every write must sit between the `transaction()` and the `commit()` — in the same house style as the deadline engine's clock guard, and that guard does catch the mutation. **Scope left where the plan put it:** the three *vrednost* columns of design §2 are not in this view (Task 6 owns valuation, and a half-built value column is worse than none), and no rok is surfaced anywhere in Task 4 — Task 3's contract that a caller owes `izvestaj_due` a **configured** filing deadline is still open and now belongs to Task 6, which is the first task that has to show one. **Review fixes (two).** (a) The čl. 8 st. 5 freeze pinned the *količina* of a signed line but not its *identity*: the `computed` UPDATE rewrote `lista_vrsta`, `sifra`, `naziv`, `vrsta`, `jedinica_mere` and `blizi_opis` unconditionally, so a line the commission counted as „Košulja“ / KOS-1 / `roba` could become a different article with the same količina and the command returned `Ok` — the potpis then attested to a document that no longer existed, and the stored `snapshot_hash` silently stopped matching. Čl. 8 st. 4 is what the commission is *handed* (nomenklaturni broj, naziv, vrsta, jedinica mere) and čl. 9 st. 1 t. 1 is what it *signs* (the count with its bliži opis), so all of it is now pinned: a Phase B write that moves any of those fields is **refused** by name (`popis_stvarno_stanje_potpisano`, naming which field moved) rather than ignored, one test case per field plus a positive control that the obračun can still price the stavka. The two čl. 8 st. 4 fields the snapshot did not cover (`vrsta`, `jedinica_mere`) were added to it, so the set the hash attests to and the set the obračun may not move are **one list** — asserted by its own test, all seven fields, mutation-verified in both directions. (b) **Req. 34 was not implemented and not declared.** `perpetual_odluka_ref` was stored as unvalidated free text under a doc comment claiming it pointed at „the completed, adopted and posted in-year popis it rests on“ — a guarantee nothing gave. The design's own coverage map puts §4 here and §8 names the test, so the gate is now implemented in `open_popis`: a non-empty reference is refused unless a `popis_sessions` row of the **same calendar year, dated before this one, with `status = 'posted'`** exists (`popis_nema_popisa_u_toku_godine`), which is čl. 9 st. 2's „у току године извршен попис … и … прокњижени“. Three tests: refused with nothing behind it, refused when the in-year popis only reached `counting`, refused when the posted popis is from another year, accepted once one is posted. **Declared deviation:** čl. 9 st. 2's *usvojen* limb — the čl. 14 st. 2 odluka o usvajanju — is **not** checked, because no schema records that decision yet. **Task 6 owns the odluka o usvajanju and owes this gate its second limb.** The field's doc comment now states exactly what is checked and what is not.

---

### Task 5: The separate lists (req. 36)

Six `lista_vrsta` values, each required rather than optional where it applies: oštećena/zastarela (čl. 10 st. 3), roba van objekta incl. on repair or with a third party (čl. 10 st. 4), **gotovina po apoenima** (čl. 11 st. 1), nedokumentovana potraživanja/obaveze (čl. 12 st. 2), and **konsignaciona roba (čl. 2 st. 5), whose signed copy must reach the owner within 10 days of the count date (čl. 2 st. 6) — its own reminder.**

- [x] **Step 1: Write the failing tests** — each list type accepts its own lines; the cash list is denominated per apoen; the consignment reminder is due exactly `datum_popisa + 10 days`; the izveštaj cannot be generated while a required list for a present category is empty.
- [x] **Steps 2–5.** Landed as `PopisLista`, `konsignacija_rok`, `nedostajuce_liste` and `ensure_liste_kompletne` in `src-tauri/src/popis.rs`, and the per-lista rules, the `liste`/`konsignacija_rok` block of the session view and `popis_provera_listi` in `src-tauri/src/commands/popis.rs`. Sixteen tests, every named behaviour with a real assertion, and **sixteen mutations run of which fifteen were caught on the first pass**; the one that was not is recorded below rather than papered over. The lista vocabulary is pinned to the v20 `popis_lines.lista_vrsta` CHECK read back off `sqlite_master` and to its own IPC wire form, for the reason the status and vrsta enums are. **Deviations and additions.** (1) **Presence is a parameter, not a stored declaration.** Req. 36 requires a posebna lista „where the category is present“, and presence is a fact about the shop that no ledger in this database holds — nothing in the books says that part of the stock is damaged, that a carton is at a third party's or that a rail belongs to somebody else. Migrations are append-only and v20 is spent, so there is nowhere to store the answer either. The person taking the popis therefore declares it and `ensure_liste_kompletne(prijavljene, sa_stavkama)` makes the declaration binding. **Contract for Task 6: call it before composing the izveštaj** — Task 5 ships the rule, not the generator, and its refusal code is `popis_prazna_prijavljena_lista`. It is reachable today through `popis_provera_listi`, which reports `spremno`, the missing liste and the same sentence the generator will refuse with, so a screen and the generator cannot word it two ways. (2) **The apoen is `cena_minor`** (para) and the count is whole pieces (milli-units, `% 1000 == 0`) — there is no apoen column and no migration to add one, and čl. 11 st. 1's apoen is exactly a signed integer minor amount. Two consequences were taken rather than left: **the blind read carries the apoen** — the one thing it carries out of the Phase B block, because on the gotovina lista `cena_minor` is not the čl. 9 st. 1 t. 5 obračunska cena but part of what the commission itself counted, and čl. 8 st. 5 withholds podatke iz knjigovodstva *o količinama*, not the commission's own count; and **the čl. 8 st. 5 potpis freezes it**, since otherwise a signed „5 × 1.000“ could become „5 × 5.000“ in the obračun with the količina untouched and the potpis would attest to a cash count nobody took (`popis_apoen_potpisan`). Both mutation-verified. (3) **No denomination whitelist.** Refusing anything outside the RSD apoeni in circulation would present a `[PRUDENTIAL]` narrowing as čl. 11 st. 1 and would refuse a lawful count of devize or of an older apoen; only „an apoen above zero“ and „whole notes and coins“ are enforced. (4) **No invented required field on the other four liste.** An owner on every konsignacija line and a reason on every oštećena line were considered and rejected: nothing in čl. 2 st. 5, čl. 10 st. 3–4 or čl. 12 st. 2 prescribes a field, this schema has no per-lista header, and a shop with one consignor names them once — a refusal here would block a lawful count in the name of a duty that does not exist. (5) The čl. 2 st. 6 reminder is **derived from the liste**, not from a switch somebody must remember to set: any konsignacija line raises it, it carries the rok, and it says plainly that the app does not deliver the lista — a reminder that read like a scheduled job would promise behaviour this module does not implement. When the rok cannot be computed from the count date the duty is reported anyway with the date it failed on, because arithmetic leaving the calendar is not an obligation ending. (6) `PopisSessionView` gained `liste` (all six with their naziv, pravni osnov and broj stavki — **including the empty ones**, since which liste a popis has *not* touched is what čl. 10–12 actually ask) and `konsignacija_rok`; the `LISTE_VRSTE` string array is gone and `save_line` decodes `PopisLista`, keeping the `popis_nepoznata_lista` refusal. (7) **One mutation was NOT caught on the first pass and is stated here:** the freeze refusal's tail differs by lista (on the gotovina lista the „cena“ is the apoen and is *not* still editable), and neutering that branch left every test green — an operator string promising an edit the very next call refuses. A test now pins both wordings and catches it. (8) **Review fix — one wire shape, one meaning.** The čl. 11 st. 1 apoen rule was judged against the payload's `cena_minor` rather than against the value the write leaves on the row, so an UPDATE of an existing cash line that only changed its bliži opis was refused with `popis_gotovina_bez_apoena` even though the COALESCE would have kept the apoen already recorded — the identical payload that means „leave the apoen alone“ in Phase B meant „you forgot the apoen“ before the potpis. `efektivni_apoen` now reads the stored value for exactly that case (existing line, gotovina lista, apoen omitted); a new stavka and a line moved onto the gotovina lista for the first time still owe the figure, and both directions are pinned by test and mutation.

---

### Task 6: The izveštaj template (req. 37)

A **structured template carrying the eight PoP čl. 13 st. 1 content elements** — not free text. Plus the odluka o usvajanju (čl. 14 st. 2) surfaced as one milestone with it.

- [x] **Step 1: Write the failing tests** — all eight elements are present as named fields; generating with any element empty is refused; the deadline shown is the computed one.
- [x] **Steps 2–5.** Landed as `IzvestajElement`, `IzvestajNarativ`, `ensure_izvestaj_kompletan`, `vrednost_minor` and `MILLI` in `src-tauri/src/popis.rs`, and `PopisPodesavanja`, `IzvestajRequest`, `IzvestajZbir`, `IzvestajView`, `compose_izvestaj` and three commands in `src-tauri/src/commands/popis.rs`. Eighteen tests (5 pure, 13 through the command layer), every named behaviour with a real assertion, and **eighteen mutations run, all eighteen caught** — including the two that make the plan's own wording true rather than decorative: dropping the čl. 8 st. 5 gate, dropping the čl. 13 st. 1 completeness gate, dropping Task 5's liste gate, replacing the configured filing deadline with a constant, valuing an unpriced stavka at zero, counting an unbooked stavka as having no razlika, computing the vrednosna razlika as „popis − knjige“ (which silently turns an unbooked cash lista into a višak), and each of the four warning limbs. **The eight elements are an enum, not eight struct fields.** `IzvestajElement::ALL` is closed for the reason every stored vocabulary in this module is closed — an element nobody modelled is one the completeness gate cannot ask for, and the shop would file an izveštaj missing a prescribed part with the suite green — and the five the commission *writes* are named fields on `IzvestajNarativ`, so req. 37's „structured template with required fields“ is a struct and not a map that may simply omit a key. **Three of the eight are computed and never typed** (`narativni()`); a gate that asked the commission to retype the stanje it had just counted would create a second copy free to drift from the liste on the next correction. The elements carry **no tačka numbers**: the register records the eight in order but this repository has not verified the article's own numbering, and „čl. 13 st. 1 t. 4)“ quoted at an inspector is a citation the shop cannot stand behind. **Deviations and additions.** (1) **The izveštaj is composed, not stored.** Nothing in this schema records one, v20 is the module's only migration and the plan budgets no second — so the document is assembled when it is asked for, `compose_izvestaj` is read-only end to end (asserted by a fingerprint over `popis_sessions.updated_at`, every line's three figures and stamp, and the potpis count — mutation-verified with a write placed *before* the gates), and the copy **says so**: „ne čuva se u aplikaciji — odštampajte ga“. A shop that typed five paragraphs and closed the screen would otherwise lose them without being told. Req. 42's retention floor therefore reaches the popisne liste, which are rows; the izveštaj lives on paper, and Task 8 owes that distinction rather than a claim to purge a document this app never held. (2) **Task 3's open contract is closed: the filing deadline is configuration.** A new `settings` key `popis` holds `rokPredajeFI` with `popis_podesavanja_get`/`_set` behind `require_admin`, validated where it is written (`popis_neispravan_datum`) because a shape the engine cannot read would otherwise surface weeks later in front of somebody who did not type it. Nothing is presumed in its place — an unset deadline refuses the annual izveštaj with Task 3's own `popis_rok_predaje_fi_nepoznat`, and a test asserts the default is `None`, that the refusal fires, and that **moving the setting moves the rok** (2027-03-31 → 30.01.2027; 2028-03-31 → 31.01.2028, the leap answer). (3) **Čl. 8 st. 5 gates the izveštaj, and this is not in the plan's named list.** The document carries the knjigovodstveno stanje and the razlike, so composing one during the count hands the commission through a *document* exactly what the blind count keeps back — the leak a hidden column never closes. Refused as `popis_izvestaj_pre_potpisa`, judged on `book_quantities_released` (both limbs) and asserted against the seeded perpetual balance appearing nowhere in the refusal. (4) **Valuation landed here, as Task 4 deferred it.** `vrednost_minor` is `količina × cena ÷ 1000` in `i128`, rounded **away from zero** at the half so a manjak is never quietly made smaller than it is, refused rather than wrapped when it will not fit — a wrapped value is a manjak reported as a višak. `MILLI` moved to `popis.rs` and the command layer's own copy now reads from it. (5) **No natural totals.** Stavke on one lista can be in komadima, metrima and kilogramima at once, so a summed količina across them is a number with no unit; the naturalna razlika stays per stavka where čl. 9 st. 1 t. 4 puts it, and what the izveštaj totals is money plus counts of viškovi and manjkova. (6) **An unvalued or unbooked stavka is reported, never zeroed.** A stavka with no cena cannot be valued and one with no knjigovodstveno stanje has no razlika; both are counted, `potpuno` says whether there were any, and two warnings name them — otherwise the gotovina lista, which has no perpetual record behind it, would print as a full višak equal to the cash in the drawer. (7) **Four warnings, none of which blocks:** the req. 40 komisija warning travels with the document that names the komisija; the čl. 9 st. 3 potpis is flagged when the computed liste are not yet signed (a warning, because drafting the izveštaj early is not itself a breach and this module does not invent duties); the incomplete-valuation pair above; and a **clock-free staleness signal** — a rok computed to fall *before* the popis was taken is a filing deadline left over from a closed business year, and it is warned about rather than refused, because a late popis is still a popis whose izveštaj has to be written. (8) **Čl. 14 st. 2 rides on the one rok**, as Task 3's contract requires: `OdlukaOUsvajanjuView` carries the same date, names the donosilac as the preduzetnik personally (čl. 4 st. 2 → ZoRač čl. 43 st. 3) and states plainly that **the application does not record the decision**. That is also the honest answer to Task 4's open second limb: čl. 9 st. 2's *usvojen* condition still cannot be checked, because there is no stored fact to check, and the two doc comments in Task 4 that promised Task 6 would close it now say so instead of deferring it again. **Review fixes (three).** (a) **A fifth warning limb was neither declared nor covered.** The čl. 2 st. 6 konsignacija reminder was pushed into the izveštaj by a branch no test judged — deleting the whole block left the popis suite green, so the ten-day owner-delivery duty could have been refactored out of the document the shop reads at the end of the count. A test now seeds a konsignacija stavka on the izveštaj popis and asserts the reminder, its computed rok (31.12.2026 + 10 = 10.01.2027) and the „ne dostavlja“ sentence, with the negative control first; both directions mutation-verified (deleting the branch and firing it unconditionally each fail exactly that test). (b) **Two uputstvo strings promised figures the composed izveštaj does not carry.** „prebrojano stanje i njegova vrednost“ and „viškovi i manjkovi, naturalno i vrednosno“ named natural quantities, while `IzvestajView` carries no količina anywhere — deviation (5) justifies not *summing* mixed units, but not a string naming what the payload lacks, which is the exact defect class this project has shipped five times. All three computed elements now say what the izveštaj does carry (vrednost plus counts of stavki) and send the reader to the popisne liste for the naturalne količine, where čl. 9 st. 1 t. 4 puts them. The wording is pinned against the fields behind it: the test scans **every key in the serialized payload, however deep**, asserts none is a količina, and then requires each of the three uputstva to say „Izveštaj iskazuje …“ and „na popisnim listama“ — so smuggling a naturalna razlika into the payload fails the same test that reverting the strings does (both mutation-verified). (c) **Req. 36's gate was opt-in from the wire.** `#[serde(default)]` on `IzvestajRequest::prijavljene_liste` meant a client that omitted the declaration sent an empty one, `ensure_liste_kompletne` — which refuses only a *declared* empty lista — passed vacuously, and the izveštaj composed with no req. 36 check at all; `popis_provera_listi` requires the identical argument for the identical rule. The default is gone, an omitted field is a wire error naming it, and an explicit `[]` still says „ništa nije prijavljeno“.

---

### Task 7: Nivelacija mode + the KEP hook (req. 33)

A retail price change raises a popis obligation (ZoRač čl. 21, PoP čl. 3) **and** posts its KEP delta (SW-9b, already shipped). The narrowed scope to only repriced articles is offered as a **`[PRUDENTIAL]` default with the reasoning visible** — neither ZoRač čl. 21 nor PoP čl. 3 scopes it, and the narrowing is borrowed from a regime this customer cannot use.

- [x] **Step 1: Write the failing tests** — a price change raises a pending nivelacija popis; its izveštaj deadline is +30 days; the narrowed scope is a default the operator can widen; the copy labels the narrowing as a preporuka, never an obaveza; the KEP entry is unaffected by the popis (they are separate obligations on the same event).
- [x] **Steps 2–5.** Landed as `NivelacijaObuhvat`, `NivelacijaObuhvatOpcija`, `NivelacijaObavestenje`, `nivelacija_obavestenje`, `nivelacija_rok_objasnjenje` and the three copy constants in `src-tauri/src/popis.rs`; `NivelacijaArtikalView`, `NivelacijaPokriceView`, `NivelacijaObavezaView`, `NivelacijaPregledView`, `NivelacijaObuhvatView`, `nivelacija_pregled`, `nivelacija_obuhvat` and two commands in `src-tauri/src/commands/popis.rs`; the hook on `kep_nivelacija` in `src-tauri/src/commands/kep.rs`; and `PopisObavezaNotice` in `src/app/kep/KepModule.tsx` with its wire types. Twelve Rust tests (3 pure, 8 command-layer, 1 on the hook) and 1 new frontend test, every named behaviour with a real assertion, and **nine mutations run, all nine caught**. **The duty and the scope are two strings and that is the whole design.** Design §7 t. 8 forbids any claim that the narrowing is required, so `NIVELACIJA_OBAVEZA` states the čl. 21 duty with its authority and the `NivelacijaObuhvat` copy offers the narrowing and says in the same breath that it „nije zakonska obaveza“, names the Pravilnik 140/2004 čl. 16 analogy **and** that that regime does not apply here, and tells the shop it may widen. A paragraph carrying both would read as „the law says count only these“. The guard is mechanical: no obuhvat string may contain a duty word (`morate`, `dužni ste`, `dužan je`, `obavezno`, `obavezan je`, `nalaže` — „obaveza“ deliberately excluded, since the copy must be able to deny one), and the label is asserted positively; mutating `pravni_status` to „obavezno sužavanje obima“ fails exactly that test. **Deviations and additions.** (1) **The obligation is derived, never auto-created.** „Raises a pending nivelacija popis“ is read as a *reported* duty and not as an auto-inserted `popis_sessions` row, because req. 39 puts the čl. 20 st. 3 usklađivanje confirmation before a popis exists at all — a session opened by the app would either bypass that gate or assert a reconciliation nobody performed. `NIVELACIJA_NAPOMENA` says so in as many words, at the till. (2) **The source is `price_history` (`source = 'update'`, non-NULL price), not `kep_entries` — and this is load-bearing.** SW-9b writes **no** ledger row when on-hand is 0 (there is no value to revalue), so an obligation derived from the ledger loses exactly that repricing silently; ZoRač čl. 21 attaches to „промене продајних цена“ and says nothing about stock, and a missed popis is a čl. 58 prekršaj. Mutation-verified: adding `AND EXISTS (… kep_entries … cause = 'nivelacija' …)` to the query fails **only** `a_repricing_with_nothing_on_hand_still_raises_the_popis`. The same source also catches a plain catalog price edit, which is equally a promena prodajne cene u maloprodajnom objektu. What it does **not** follow — akcijske cene from kampanje, prices from an article import — is named in `NIVELACIJA_IZVOR` rather than left implied: a list that reads as complete and is not would have a shop conclude it owes nothing. (3) **Only a `posted` nivelacija popis discharges an obligation.** Čl. 21 requires the popis *and* the usklađivanje stanja, and čl. 14 st. 3's knjiženje is where this module records that; a popis merely opened is reported as `popis_u_toku` and clears nothing, since a draft somebody abandoned would otherwise silence the reminder for good. (4) **The scope list is handed over, never written.** Seeding the popisne liste from the chosen scope was considered and rejected: `stvarna_kolicina_milli` is NOT NULL, so a seeded stavka would carry a count of zero that nothing distinguishes from a counted zero — a manjak of the whole stanje, on a document that is evidence. What `nivelacija_obuhvat` returns is exactly the čl. 8 st. 4 artefact („листе са номенклатурним бројевима, називима, врсти и јединицама мере“), four fields and **no količina from any source**; the counted state stays the commission's to write through `save_line`. (5) **Req. 29 reaches this screen, and it is the earliest one there is.** The scope list is read before anything is counted, so a perpetual stanje beside each article would hand over the book quantities at the very start of the count — the leak no later potpis can unring. Asserted behaviourally (the seeded balance appears nowhere in the serialized answer, under either scope, with `knjigovodstvo_dostupno` first asserted false so it cannot pass vacuously) and structurally (no statement in the region may name `inventory_balances` or `inventory_movements`); adding an „očekivano“ column fails **both** and nothing else. The `CeoObjekat` scope is `products.active = 1` for the same reason — filtering on „what the books say we have“ would leak stock through the mere presence of a line. (6) **The KEP hook returns the notice.** `kep_nivelacija` now answers with `NivelacijaObavestenje` instead of `()`. Composed from constants after the commit, it reads no row and no clock, so nothing about it can fail a nivelacija whose price and KEP Δ are already durable — and `the_popis_and_the_kep_entry_are_two_obligations_on_one_event` pins the converse: walking a nivelacija popis to `posted` leaves the ledger fingerprint (redni broj, iznos, kind, cause) byte-identical. (7) **The notice is rendered, not dropped.** A return value every caller discards would make the doc comment's „the moment the shop changes a price is the only moment it is certain to be looking“ a promise the code does not keep, so `KepModule.tsx` shows it as a persistent block — not a toast, because this is a duty with a rok and not a confirmation — and it survives `resetForm`. Every string in it comes from the backend, so the duty has one wording and not two; a storno that moves no price raises no notice, which its own test pins. The `src/app/popis/*` module stays Task 9's. (8) The rok sentence is built with `format!` from `IZVESTAJ_ROK_DANA_PO_POPISU` rather than typed beside it, and says the 30 days run **from the popis, not from the price change**: a shop reading the rok off the nivelacija would file late while believing it was early. `a_nivelacija_popis_opened_as_such_is_due_thirty_days_after_the_count` walks the real path — `open_popis` with `vrsta: Nivelacioni`, no rok za predaju FI configured — where Task 6's existing rok test edits the column directly.

---

### Task 8: `legal.rs` + retention

Add the popis notice with the **preduzetnik tier verified from the memo** — and if §3 V5 does not state a preduzetnik figure, render `None` for that tier rather than guessing, and record it as a deviation. Retention floor **5 years** (ZoRač čl. 28 st. 7, counted from the last day of the business year per st. 9), with the panic-safe floor pattern from SW-9c.

- [ ] **Steps 1–5.**

---

### Task 9: Frontend — the count sheet and the module

- [ ] **Step 1: Write the failing tests**

```tsx
  it("renders no book-quantity and no difference column during counting", () => {
    render(<CountSheet status="counting" lines={lines} />);
    expect(screen.queryByText(/knjigovodstven/i)).not.toBeInTheDocument();
    expect(screen.queryByText(/razlika/i)).not.toBeInTheDocument();
  });

  it("reveals them only after the phase A signature", () => {
    render(<CountSheet status="countedSigned" lines={linesWithBook} />);
    expect(screen.getByText(/knjigovodstven/i)).toBeInTheDocument();
  });

  it("warns but does not block on a goods-handling commission member", () => { /* req. 40 */ });

  it("offers no edit affordance on a posted popis", () => { /* req. 41 */ });

  it("labels the nivelacija scope narrowing a preporuka, never an obaveza", () => { /* req. 33 */ });
```

- [ ] **Steps 2–5.**

---

### Task 10: Docs + full gate run

- [ ] Flip SW-16 in `docs/SERBIAN-LAW-COMPLIANCE.md` §3, recording the blind-count constraint and the three open items (R-5, R-6, R-7).
- [ ] Update `docs/PROGRESS.md` with real counts.
- [ ] Run all six gates; report exact counts. Commit.

---

## Self-review

**Spec coverage.** §1 → Tasks 1, 2, 4. §2 → Tasks 1, 9. §3 → Task 7. §4 → Task 4. §5 → Tasks 5, 6. §6 → Tasks 3, 4, 8. §7 → enforced by the Task 2 and Task 9 assertions. §8 → distributed.

**Placeholders.** Tasks 4–8 name required behaviours rather than pasting bodies, following the established repo seeding pattern; every behaviour is a concrete assertion. Tasks 2, 3 and 9 carry full code.

**Type consistency.** `PopisStatus` / `PopisEvent` (Task 2) are consumed in Tasks 4, 9. `izvestaj_due` (Task 3) is consumed in Tasks 6, 7. `lista_vrsta` (Task 1) is consumed in Task 5.

**One gap found during review:** Task 2 originally exposed `book_quantities_visible` as a UI helper. That would have made the čl. 8 st. 5 property a rendering decision. It is now the state machine's own predicate, and Task 4 asserts the **query layer** refuses a Phase A read that would return book quantities — so hiding a column can no longer be mistaken for compliance.
