# Serbian Law Compliance — VantumPOS

**Prepared for the founder of Actaer (com.actaer.vantumpos). State of law: 31 July 2026.** Every load-bearing statement below survived adversarial verification against primary sources; claims that were corrected during verification appear here in corrected form only. Items that could not be settled in writing are quarantined in §6.

> **Revision 31.07.2026 — penalty-tier sweep.** Verifying SW-11/SW-15 exposed a **systemic** defect: fine amounts had been read off the opening stav of a kaznene-odredbe article (the *pravno lice* tier) and attributed to the pilot, which is a **preduzetnik**. Nine rows were corrected (3, 9, 12, 14, 21, 27, 28, 29, 32), plus §1a's central risk paragraph, §1b, SW-3, SW-11, SW-15 and F-3. **A preduzetnik cannot commit a privredni prestup at all** (ZPP čl. 6 st. 1) — any row saying otherwise was wrong as applied. Full working: [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §1. When adding any new fine figure to this document, state the tier and the stav.

**Source legend (linked inline throughout):**
ZF = Zakon o fiskalizaciji (Sl. glasnik RS 153/2020, 96/2021, 138/2022) · Uredba = Uredba o registru elemenata EFU (32/2021) · TU = PURS Tehničko uputstvo ESIR/L-PFR v1.17 (09.08.2024) · PVFR = Pravilnik o vrstama fiskalnih računa (31/2021…57/2022) · ZPPPA = Zakon o poreskom postupku (…109/2025) · ZZPL = Zakon o zaštiti podataka o ličnosti (87/2018, unamended) · ZoT = Zakon o trgovini (52/2019, 35/2026) · PEP = Pravilnik o evidenciji prometa / KEP (99/2015, 44/2018) · ZoRač = Zakon o računovodstvu (73/2019, 44/2021) · PoP = Pravilnik o popisu (89/2020) · ZPDV = Zakon o PDV (…109/2025) · ZZP26 = novi Zakon o zaštiti potrošača (35/2026) · ZZP21 = Zakon o zaštiti potrošača (88/2021) · ZoR = Zakon o radu (…109/2025) · ZEOR = Zakon o evidencijama u oblasti rada (SRJ 46/96…) · AML = Zakon o sprečavanju pranja novca (…19/2025) · **Zakon 68/2015** = Zakon o obavljanju plaćanja pravnih lica, preduzetnika i fizičkih lica koja ne obavljaju delatnost (68/2015, no amendments) + Pravilnik 77/2011 — **never abbreviate this "ZOP"**, which conventionally denotes the repealed *Zakon o platnom prometu* · **ZPP** = Zakon o privrednim prestupima (…101/2005) · **ZoP** = Zakon o prekršajima · ZEF = Zakon o elektronskom fakturisanju (…109/2025) · ZEO = Zakon o elektronskim otpremnicama (94/2024, 109/2025) · ZAG = Zakon o arhivskoj građi (6/2020)

---

## 1. Verdicts on the two headline questions

### 1a. ESIR-handoff legality — "may a non-certified back-office hand sales to a certified ESIR?"

**Verdict: defensibly YES on written sources, with one narrow recharacterization risk, no regulator statement either way, and hard operational constraints. The current pilot posture (companion back-office, shop double-rings on its own ESIR) is lawful today with no open questions.**

What written sources establish (all verified against primary texts):

- **Approval attaches per element, not to surrounding software.** An ESIR is the element "čija je upotreba odobrena od strane Poreske uprave, u koji obveznik fiskalizacije unosi podatke o prometu i iz kojeg se izdaje fiskalni račun" — [ZF čl. 2 st. 1 tač. 5](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html) (definition is cumulative: data entry AND receipt issuance). The statute regulates only the obveznik, the dobavljač EFU, and the approved elements (ESIR + PFR, [ZF čl. 6 st. 1–2](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html)); it nowhere defines or regulates software that prepares data upstream of an ESIR and issues no receipts. The public Registar records nothing about integrations, callers or data sources ([Uredba čl. 8, 11](https://www.paragraf.rs/propisi/uredba-o-obliku-i-sadrzaju-registra-elemenata-vrstama-elektronskih-fiskalnih-uredjaja.html); [live registry](https://www.purs.gov.rs/sr/eFiskalizacija/registar-odobrenih-elemenata-efu.html)).
- **Composite architectures are expressly contemplated.** The Uredba enumerates an ESIR "objedinjen u okviru softvera za planiranje resursa" (ERP-embedded) and contains an open clause for "drugi oblici" — [Uredba čl. 6 st. 1 tač. 2 i st. 2](https://www.paragraf.rs/propisi/uredba-o-obliku-i-sadrzaju-registra-elemenata-vrstama-elektronskih-fiskalnih-uredjaja.html).
- **PURS's own approval questionnaire contemplates externally-fed ESIRs.** File-based transaction input is a declarable optional ESIR feature ("P17: ESIR podržava unos podataka o transakciji preko fajla… — Opciono") and human operator input is likewise optional (P1 — Opciono) — [TU 7.4.2 tač. 10](https://purs.gov.rs/upload/media/2025/2/4/414144/Tehnickouputstvo-ESIRiliL-PFR.pdf). The prohibited-functions list (TU 7.4.2 tač. 9) contains exactly three items, all about not issuing/altering data without/from the PFR; **nothing prohibits an ESIR receiving sale data from external software** (verified by exhaustive read of the 54-page text).
- **The pattern is market-mainstream.** Softek ESIR Link is marketed as connecting "Vaše postojeće POS/ERP rešenje… bez potrebe za sertifikaciju vašeg softvera" (file-drop IN/OUT; Softek ESIR IB 338, approved 05.01.2022, never revoked) — [softek.rs](https://www.softek.rs/e-fiskalizacija/esir-link-za-programere/); Teron publishes a public REST API to its certified ESIR/LPFR — [api.teron.rs](https://api.teron.rs/). This is practical comfort, **not legal authority** — PURS approved the products, never blessed the abstract pattern.

The residual risk: a strict reading of [ZF čl. 2 st. 1 tač. 5](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html) ("u koji obveznik unosi podatke o prometu") lets an inspector argue that where the sale is composed, priced and finalized in VantumPOS and the approved ESIR is a pass-through, VantumPOS is the de facto unapproved ESIR. Consequences — **penalty tier CORRECTED 31.07.2026, see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §1 row 4:** shop — prekršaj **50.000–500.000 RSD for a preduzetnik** ([ZF čl. 15 **st. 3**](https://www.purs.gov.rs/upload/media/2025/2/4/382229/Zakonofiskalizaciji.pdf)); the 300.000–2.000.000 figure is [ZF čl. 15 st. 1](https://www.purs.gov.rs/upload/media/2025/2/4/382229/Zakonofiskalizaciji.pdf), which opens *"kazniće se za prekršaj **pravno lice** - obveznik fiskalizacije"* and does not reach the pilot. The offence tačka (st. 1 tač. 3) is right. Separately, zabrana vršenja delatnosti **15 dana / 90 dana / jedne godine** per premises ([ZF čl. 12](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html) — statute says "jedne godine", **not** "365 days") is an inspection measure available against any obveznik incl. a preduzetnik, but its **predicate is only failure to evidence each individual retail sale through the EFU (čl. 12 st. 1)** — it does **not** attach to a recharacterization or registry-check failure, so it is not a consequence of this risk. (A separate privremena zabrana for ignoring a rešenje sits in čl. 13 st. 4.) Actaer — dobavljač fine, fixed 300.000 RSD pravno lice / 150.000 preduzetnik ([ZF čl. 18 u vezi čl. 6 st. 6](https://propisi.net/zakon-o-fiskalizaciji/)) — verified correct. **No PURS FAQ answer or MF mišljenje exists on this question in either direction** (verified: exhaustive FAQ grep 16.07.2026 — the only ERP item concerns reprint-as-Kopija; budiefiskalizovan.gov.rs refused connections). This is precisely the gap to close with a written pitanje before scaling (§4, §6).

**Hard constraints that survive regardless:** every retail sale must be evidenced through the EFU with the fiscal receipt issued **at the moment of sale** ([ZF čl. 4 st. 2](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html), [čl. 6 st. 1, čl. 8 st. 1](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html)) — batch/end-of-day fiscalization is illegal; any future integration must be per-sale, through the target ESIR's **documented** input channel, after verifying its name+version+IB and absence of rešenje o ukidanju in the [Registar](https://www.purs.gov.rs/sr/eFiskalizacija/registar-odobrenih-elemenata-efu.html).

**Safe posture rules (binding on product and marketing until PURS answers):**
1. VantumPOS issues no receipts, prints nothing receipt-like, never talks to a PFR, never holds a bezbednosni element.
2. Pilot workflow is "ESIR first, VantumPOS second"; every sale rings the shop's certified ESIR at the till. Any request to "skip the ESIR for small sales" is refused in writing.
3. No output may resemble a fiscal receipt (no QR, no "FISKALNI RAČUN" framing, no PIB/PFR-number/brojač block); any future sale-itemizing print carries "OVO NIJE FISKALNI RAČUN" at ≥2× font, top and bottom ([PVFR čl. 2 st. 8–10](https://www.paragraf.rs/propisi/pravilnik-o-vrstama-fiskalnih-racuna-tipovima-transakcija-nacinima-placanja-pozivanju-na-broj.html); [TU deo 16 P10](https://purs.gov.rs/upload/media/2025/2/4/414144/Tehnickouputstvo-ESIRiliL-PFR.pdf)).
4. No feature usable for turnover suppression — this is the criminal red line: producing, selling or even **holding** software "koji služi za izbegavanje evidentiranja prometa, odnosno izbegavanje plaćanja poreza" is 1–5 years imprisonment + 1–5 year professional ban + confiscation ([ZPPPA čl. 175b, also 175a](https://www.paragraf.rs/propisi/zakon_o_poreskom_postupku_i_poreskoj_administraciji.html), in force 20.12.2022). Note "drži" criminalizes the pilot shop's possession too — the clean design protects both sides.
5. Marketing/contract language states expressly: "VantumPOS nije elektronski fiskalni uređaj niti ESIR i ne izdaje fiskalne račune; promet na malo se evidentira preko odobrenog ESIR-a prodavnice." Mere absence from the PURS register is NOT unlawful ([ZPPPA čl. 175a](http://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/13_saziv/2506-22%20lat..pdf) makes the evasion function, not non-registration, the constitutive element) — but the sentence removes the čl. 18 predicate.

Helpfully, an internal parallel sales/stock ledger is not merely tolerated — core elements are **mandatory** for the trader ([ZoT čl. 29–30](https://www.paragraf.rs/propisi/zakon_o_trgovini.html); KEP may be kept electronically, [PEP čl. 10](https://www.paragraf.rs/propisi/pravilnik_o_evidenciji_prometa.html)). VantumPOS's category is legally required software with no form or approval requirement.

### 1b. ZZPL — is Actaer a processor, and what must the DPA + software contain?

**Verdict: Actaer is NOT an obrađivač (processor) by default for a local-first app. It becomes one the moment its staff view or touch the shop's personal data on the shop's behalf — which describes AnyDesk support sessions. Recommendation: sign the čl. 45 DPA before the pilot regardless; it is cheap, matches Serbian vendor practice, and protects both parties.**

- **Default: not a processor.** ZZPL defines obrađivač as one who "obrađuje podatke o ličnosti u ime rukovaoca" and obrada as actions performed **with** personal data — delivering software the shop runs on its own machine, with no vendor access, is not obrada ([ZZPL čl. 4 st. 1 tač. 3, 9](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html)). But obrada expressly includes **uvid** (viewing) and upotreba (use) — so an AnyDesk session in which Actaer staff can see users/sales/shift data IS processing on the shop's behalf. Hosted backups would be unambiguous processing ("pohranjivanje").
- **The line, per the standard interpretive aid:** [EDPB Guidelines 07/2020 para 83 + examples](https://www.edpb.europa.eu/system/files/documents/2023-10/EDPB_guidelines_202007_controllerprocessor_final_en.pdf) — an IT provider that "inevitably… systematically has access to personal data" is a processor ("a processor agreement is therefore concluded"); an "IT-consultant fixing a software bug" with "purely incidental… very limited" access is not, and the controller instead applies Art 32/čl. 50 security measures. Serbian vendor practice (Seyfor/Saop GTC) papers support-with-access as processor activity. Persuasive, not binding; the ZZPL provisions interpreted are near-verbatim GDPR transpositions (čl. 4, 45, 50).
- **DPA content is fully prescribed** by [ZZPL čl. 45](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) — the complete checklist is in §4. Correct stav numbering (verified): sub-processor flow-down + full liability = **st. 7**; processor-turned-controller = **st. 8**; Poverenik's standard contractual clauses power = st. 11 (basis of the SCC Odluka, Sl. glasnik RS 5/2020 — usable as a template). Statutory term is "vrsta lica", not "kategorije lica".
- **Software duties bite directly:** čl. 50 binds controller AND processor (tehničke, organizacione i **kadrovske** mere; **kriptozaštita named first**; instruction-only access st. 5); čl. 42 privacy-by-design/default is directly sanctionable — **tier CORRECTED 31.07.2026:** 50.000–2.000.000 RSD is [čl. 95 **st. 1** tač. 20](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html), the *pravno lice* tier; a **preduzetnik faces 20.000–500.000 (čl. 95 st. 4)**. Encrypted backups additionally unlock the čl. 53 st. 3 tač. 1 notification safe-harbor for lost media.
- **Shop-side essentials:** lawful basis for employee data is contract + legal obligation ([čl. 12 st. 1 tač. 2–3](https://mduls.gov.rs/wp-content/uploads/Zakon-o-za%C5%A1titi-podataka-o-li%C4%8Dnosti.pdf)), NOT consent (restrictively construed in employment — build **no** consent UI); the shop keeps čl. 47 records despite its size because daily POS processing "nije povremena" (st. 9 tač. 2; **fine tier CORRECTED 31.07.2026** — the fixed 100.000 RSD of čl. 95 st. 2 tač. 5 binds a rukovalac *"koji ima svojstvo pravnog lica"*; a **preduzetnik faces fixed 50.000, čl. 95 st. 6**); employees get a čl. 23 notice naming Actaer as recipient during support. Breach chain: processor→controller bez nepotrebnog odlaganja (čl. 52 st. 3); controller→Poverenik ≤72h on the prescribed form ([Pravilnik 40/2019](https://www.paragraf.rs/propisi/pravilnik-o-obrascu-obavestavanja-poverenika-za-informacije-od-javnog-znacaja-o-povredi.html), scanned copy to povredapodataka@poverenik.rs); document every breach internally (čl. 52 st. 6–7); data subjects only on high risk (čl. 53). **No DPO required** for either party (čl. 56 st. 2) — do not voluntarily name one (triggers publish-and-notify duties); no database registration with the Poverenik exists under the 2018 law.

---

## 2. Binding requirements register

Status column reflects the software state **per row, as at the date stamped in that cell** — an undated cell is the original 16.07.2026 baseline (no printing, no KEP/kalkulacija/nivelacija/popis/reklamacije/price history, unencrypted local backups, screen-only post-sale dialog, no customers table). Rows 21, 27, 28 and 29 were re-stated on 31.07.2026 when SW-11 and SW-15 shipped; rows 22, 22a and 23 the same day when SW-7 shipped; rows 14 and 15 were re-stated on 01.08.2026 when SW-14 shipped; rows 8, 9, 10, 11 and 12 were re-stated on 02.08.2026 when the ZZPL trio (SW-10, SW-13, SW-17) shipped; row 23 again on 02.08.2026 when its čl. 63 st. 3 residual closed; row 19 was re-stated on 03.08.2026 when SW-16 shipped, and again on 07.08.2026 — together with the §3 SW-16 row — when the popisne liste, the odluka o popisu and the plan rada gained an export (reqs. 31/32/35).

> **Revision 07.08.2026 — the understatement sweep. Rows 6, 17, 18, 20 and 24 were re-stated.** Every one of them still carried its 16.07.2026 baseline *„Gap“* for work that shipped between 17.07. and 03.08.2026, and on 07.08.2026 that staleness led a survey agent to rank two long-fixed defects as pilot blockers. **This is the mirror of the false-promise class this document has corrected six times, and it is not the harmless half:** a row that denies a control the software runs withdraws the reader's only pointer to it, and an inspector reading this register would be told the shop has nothing where it has a KEP. Each of the five was re-verified against the named symbol before it was re-stated, and **none was flipped to a tick** — every one of them is a partial, and each now states what within the row is still not built. Rows 16 and 21 were read in the same pass and **deliberately left alone.** Row 21's residual is already current. Row 16 is the one this sweep could not settle: `kep_kalkulacija.rs` does generate a goods-receipt document, but čl. 29 st. 1 is a duty to **possess the supplier's isprava**, and the `kalkulacije` table (v13) carries no adresa, no matični broj/BPG and no supplier document broj/datum — so *„no goods-receipt documents“* understates the code while a tick would overstate it, and re-stating it needs the čl. 29 field-by-field read that [KEP-VERIFIED-RULES.md](KEP-VERIFIED-RULES.md) supports and this pass did not perform. It stays as it is until that read is done.

| # | Obligation | Binds | Legal basis | Status in VantumPOS today | Required action |
|---|---|---|---|---|---|
| 1 | Evidence every retail sale (incl. avansi) through the EFU at the moment of sale; recording elsewhere does not discharge it | Shop | [ZF čl. 4 st. 2, čl. 6 st. 1](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html) | N/A (posture) | Posture rule: ESIR-first double-ring; UI nudge (SW-5) |
| 2 | No non-fiscalized "račun" at moment of sale; fiscal receipt only via approved elements; receipt data to PURS in real time | Shop | [ZF čl. 15 st. 1 tač. 1, 3, 5; čl. 8 st. 1](https://www.purs.gov.rs/upload/media/2025/2/4/382229/Zakonofiskalizaciji.pdf) | Compliant (screen-only dialog) | Keep no printable receipt-like output; banner (SW-1) |
| 3 | Zabrana vršenja delatnosti **15 dana / 90 dana / jedne godine** (statute's own words — **not** "365 days"; 24-month window, per premises) for unrecorded turnover. **Predicate is narrow (CORRECTED 31.07.2026):** čl. 12 st. 1 triggers only on failure to evidence **each individual retail sale** through the EFU — it does not attach to any other ZF breach. A separate *privremena zabrana* for non-compliance with a rešenje sits in **čl. 13 st. 4** | Shop | [ZF čl. 12 st. 1–3; čl. 13 st. 4](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html) | N/A | Cashier training; documented refusal of skip-ESIR requests (F-8) |
| 4 | Supplier must deliver only approved EFUs; fixed fines 300k/150k/50k RSD | Vendor | [ZF čl. 6 st. 6, čl. 18](https://propisi.net/zakon-o-fiskalizaciji/) | Compliant (not supplied as EFU) | Disclaimer sentence in contracts/marketing (F-6) |
| 5 | Criminal ban on producing/selling/**holding** software serving turnover/tax evasion (1–5 yrs + ban + confiscation) | Vendor + shop (holder) | [ZPPPA čl. 175a, 175b](https://www.paragraf.rs/propisi/zakon_o_poreskom_postupku_i_poreskoj_administraciji.html) | Largely compliant (append-style ledger); reset needs logging/tombstone | Anti-evasion invariants audit (SW-4); posture doc (F-7) |
| 6 | Non-fiscal documents: "OVO NIJE FISKALNI RAČUN", unerasable, ≥2× font; such docs are not receipts | Formally EFU docs; adopt defensively | [PVFR čl. 2 st. 4, 8–10](https://www.paragraf.rs/propisi/pravilnik-o-vrstama-fiskalnih-racuna-tipovima-transakcija-nacinima-placanja-pozivanju-na-broj.html) | **Re-stated 07.08.2026 — the banner shipped with SW-1; what is still open is the export limb, which has no subject yet.** `NonFiscalBanner` (`src/app/register/RegisterScreen.tsx:1289`) renders **twice** in the post-sale dialog — above the stavke and below the ESIR nudge (`:922`, `:983`) — as a `role="note"` block in `text-2xl font-bold uppercase`, and its source comment carries the čl. 2 st. 8–10 design rule out loud: never add a QR, a „FISKALNI RAČUN“ heading or a PIB/PFR/brojač block to this or any future export. The same banner heads the receipt detail panel (`src/app/ReceiptsScreen.tsx:632`, `text-xl` over a `text-xs` stavka table); the dialog subtitle reads „Interni pregled prodaje — nije fiskalni račun“ and the ESIR nudge „Ovaj interni račun ne zamenjuje fiskalni račun.“ Pinned by `RegisterScreen.test.tsx:471` (both copies, inside the dialog) and `ReceiptsScreen.test.tsx:263`. **Two limbs are not closed.** SW-1's *„on every current/future export that itemizes a sale“* has **no subject in this build**: nothing the crate writes to a file or hands to `PrintService` itemizes a sale — `reports_export_csv` is daily/shift aggregate, and the print stack serves popis, KEP, reklamacije and the privacy exports — so it stands as a design rule with nothing to test, and the first sale-itemizing export must carry the banner with it. And the **≥2× ratio is asserted by presence, never measured**: the two surfaces use `text-2xl` / `text-xl` against `text-xs`–`text-sm` rows, and no test computes the ratio the Pravilnik states | SW-1 ✓ (post-sale dialog + receipt detail); the export limb has no subject yet |
| 7 | Written processing contract when Actaer processes for the shop (support access, backups) | Vendor + shop | [ZZPL čl. 45](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) | N/A software | DPA per §4 checklist (F-1) |
| 8 | Security of processing: kriptozaštita, CIA+resilience, rapid restore, regular testing, instruction-only access | Shop + vendor | [ZZPL čl. 50 st. 1, 2, 5](https://mduls.gov.rs/wp-content/uploads/Zakon-o-za%C5%A1titi-podataka-o-li%C4%8Dnosti.pdf) | **Re-stated 02.08.2026.** Partial: argon2, roles, WAL, auto-backups ✓; access trail ✓ — hash-chained `audit_events` (v18) read and exported from Privatnost → Evidencija pristupa, and the st. 5 instruction-only leg is the `support_sessions` nalog. **Both are held out as prudential/evidential, never as a čl. 50 duty** — čl. 50 is absent from čl. 95, so no prekršaj is prescribed for it. Kriptozaštita exists but is **opt-in**: `backup_crypto.rs` wraps the snapshot in XChaCha20-Poly1305 with an argon2id-derived key **only once an admin sets a backup passphrase** — with no passphrase the backup file is still plaintext SQLite, so the st. 2 tač. 1 measure is available rather than in force. No regular restore testing either | SW-2 |
| 9 | Records of processing (controller 7 items / processor 4), kept permanently, shown to Poverenik; small-shop exemption fails ("obrada nije povremena"). **Penalty tier CORRECTED 31.07.2026:** čl. 95 **st. 2** is expressly *"rukovalac … koji ima svojstvo **pravnog lica**"* → fixed 100k binds a d.o.o. only. **Preduzetnik: fixed 50.000 (čl. 95 st. 6)** | Shop; Actaer if processor | [ZZPL čl. 47 st. 1, 4, 7–9; čl. 95 st. 2 tač. 5 (pravno lice) / **st. 6** (preduzetnik)](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) | **Re-stated 02.08.2026 — now generated.** `cl47.rs` writes `processing_activities` (v18) at every launch and on demand (`cl47_generate` / `cl47_export`, Privatnost → Radnje obrade), driving the st. 1 t. 6 rok **per vrsta podataka** out of the shared `retention_policies` rows so the register cannot claim a period the till does not apply; the export names the čl. 47 **st. 4** obrađivač record as a different document kept by Actaer, not by the shop. `docs/compliance/evidencija-obrade-cl47.md` remains the hand-kept narrative version, pinned by `docs_guard` | F-4 |
| 10 | Breach chain: processor→controller without delay; controller→Poverenik ≤72h on prescribed form; internal breach documentation; data subjects on high risk | Both | [ZZPL čl. 52 st. 1–7, čl. 53; Pravilnik 40/2019](https://www.paragraf.rs/propisi/pravilnik-o-obrascu-obavestavanja-poverenika-za-informacije-od-javnog-znacaja-o-povredi.html) | **Partial, re-stated 02.08.2026 (SW-17).** The software half is built: `data_breaches` (v18) provides the record the rukovalac must keep under čl. 52 st. 6–7 — every povreda is logged first and notifiability is a derived flag, the `saznanje_at` anchor is immutable at the storage layer, the st. 2 delay reason becomes mandatory once 72 h have passed, and the čl. 53 block records whether the affected individuals were told or which st. 3 exception was relied on. `breaches_export_obrazac` renders the Pravilnik 40/2019 obrazac for print/scan — **there is no submission API and none exists**; the scan goes to povredapodataka@poverenik.rs. **The chain itself is a founder action, not code:** the processor→controller leg needs the DPA (F-1) and the human runbook (F-12) | DPA clause (F-1); runbook (F-12) |
| 11 | čl. 23 privacy notice to employees at collection (recipients incl. Actaer; rights; retention) | Shop | [ZZPL čl. 23](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) | **Shipped** — `docs/compliance/obavestenje-zaposlenima.md`, re-issued 01.08.2026 for the SW-14 working-time record and again **02.08.2026** for the evidencija pristupa, the daljinska-podrška nalog and the credential purge (SW-10 req. 9, req. 27) | F-3 |
| 12 | Privacy by design/default — directly sanctionable. **Tier CORRECTED 31.07.2026:** 50k–2M is čl. 95 **st. 1** (pravno lice). **Preduzetnik: 20.000–500.000 (čl. 95 st. 4).** Same split applies to a čl. 5 principle breach, whose tačka is **čl. 95 st. 1 t. 1** | Shop (software determines capability) | [ZZPL čl. 42, čl. 95 st. 1 tač. 20 (pravno lice) / **st. 4** (preduzetnik)](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) | **Re-stated 02.08.2026 (SW-10 + SW-13).** Roles ✓; the čl. 5 st. 1 t. 5 purge is now time-driven and runs at launch and every 6 h (`commands/personnel.rs::purge_expired_classes`) ✓; employee data is split into three physical classes so the purge reaches only credentials and the access log — `PurgeableClass` has no variant for the ZEOR register, which makes it a type error rather than a review catch ✓; pseudonimizacija per čl. 42 st. 1 t. 1 — no `ime`/`prezime`/JMBG is denormalised onto a transaction row ✓. Still open: backup encryption is opt-in rather than default (row 8) and remote-support masking of the absence-reason column is not built (SW-14 req. 28) | SW-2 |
| 13 | Lawful basis = contract/legal obligation, not consent; no consent UI for employee data | Shop | [ZZPL čl. 12 st. 1 tač. 2–3](https://mduls.gov.rs/wp-content/uploads/Zakon-o-za%C5%A1titi-podataka-o-li%C4%8Dnosti.pdf) | Compliant (no consent UI exists) | Keep it that way |
| 14 | **Daily overtime record — VERIFIED 01.08.2026, see [SW14-VERIFIED-RULES.md](SW14-VERIFIED-RULES.md) §3 W1.** ZoR čl. 55 st. 6 requires a **daily record of OVERTIME only** — Serbia imposes **no** general daily record of all working time (unlike HR, FBiH and MK, which is the likely source of the "clock-in/out" framing). Tier confirmed **preduzetnik 50.000–150.000**; cite precisely as **čl. 276 st. 1 (uvodna rečenica) u vezi sa tač. 1a)** — there is **no** separate preduzetnik stav in čl. 276, unlike čl. 273/274/275 which each put it in st. 2, and that drafting asymmetry is exactly why the original error happened. **The register is the SMALLER exposure:** ordering overtime beyond the čl. 53 caps (≤8 h/week, ≤12 h/day incl. overtime) is **čl. 274 st. 1 tač. 3 → preduzetnik 200.000–400.000**, ~2.7× larger and previously absent from this register. Inspector: **inspektor rada**; čl. 273–276a prescribe **no** zaštitna mera | Shop | [ZoR čl. 53, čl. 55 st. 6, čl. 274 st. 1 tač. 3 + st. 2, čl. 276 st. 1 tač. 1a](https://www.paragraf.rs/propisi/zakon_o_radu.html) | **Shipped 01.08.2026 (SW-14)** — one row per (employee, calendar date) in `work_time_entries` (v17), append-only with versioned corrections; `worktime.rs::assess_caps` enforces the two **čl. 53** caps (st. 2 ≤ 8 h prekovremenog u kalendarskoj nedelji, st. 3 ≤ 12 h dnevno ukupno) **and only those** — it hard-codes the preraspodela leg to `false` because it cannot see whether the employee is in preraspodela; the **čl. 57 st. 5 60 h/week ceiling is applied by the caller, `commands/worktime.rs::assess_caps_for_employee`**, which is where the branch is taken; both fines are tier-resolved in `legal.rs` | SW-14 |
| 15 | **ZEOR — VERIFIED + CORRECTED 01.08.2026, see [SW14-VERIFIED-RULES.md](SW14-VERIFIED-RULES.md) §1.** ZEOR **is still in force** but is a **torso**: čl. 8–22 and čl. 26–40 ceased on 23.05.2009 (ZZOSN čl. 110). What survives for an employer: evidencija o zaposlenim licima (čl. 5–7), **evidencija o zaradama (čl. 23–25)**, invalidsko (čl. 41–43), plus čl. 3–4 and 44–52. A preduzetnik is bound only via **čl. 3 st. 1** as a *"fizičko lice koje ima zaposlene"*. ZEOR prescribes **classified period totals** (čl. 24 tač. 1), **not** daily timestamps — the only *daily* duty in Serbian law is ZoR čl. 55 st. 6 and it covers **overtime only**. Retention `trajno` attaches to the **derived, period-closed** classification (čl. 25 st. 3 / čl. 7 st. 2), **not** to raw punch events — čl. 4 st. 2 is continuity of maintenance, not a retention rule. **Penalty: the earlier "500k–1M" was again the *pravno lice* tier (čl. 50 st. 1) — the 31.07.2026 sweep missed this row.** čl. 51 names *"fizičko lice koje ima zaposlene"* at 300.000–500.000, but that **exceeds the ZoP čl. 39 st. 1 tač. 1 ceiling** for that class → **UNRESOLVED; print NO ZEOR figure to a customer** (§6 item 19). Methodology/codes ARE prescribed here (unlike ZoR — čl. 44 + Jedinstveni kodeks šifara), so "layout is free" must **not** be carried across from row 14 | Shop | [ZEOR čl. 3 st. 1, čl. 23, čl. 24 tač. 1, čl. 25 st. 3, čl. 44, čl. 50–51](https://www.paragraf.rs/propisi/zakon_o_evidencijama_u_oblasti_rada.html) | **Partial — hour side shipped 01.08.2026 (SW-14)**: the fifteen čl. 24 tač. 1 buckets are columns on `work_time_entries`, `zanimanje_sifra` / `kvalifikacija_sifra` are stored as codes (čl. 44 st. 2), and the derived period-closed classification is `trajno` + `never_purge` in `retention.rs`, which the go-live reset asserts intact. **Still out of scope by standing rule (F-12):** čl. 24 tačke 2–3 (zarade) and the čl. 5–7 evidencija o zaposlenim licima. No ZEOR fine figure is rendered | SW-14; SW-3 |
| 16 | **Possess isprave o robi — VERIFIED 19.07.2026, see [KEP-VERIFIED-RULES.md](KEP-VERIFIED-RULES.md).** čl. 29 st. 1 data: poslovno ime, adresa, PIB + matični broj/BPG; doc broj + datum; naziv/merna jedinica/količina; **nabavna cena; zaduženje za vlastitu robu; prodajna cena.** Missing isprave (čl. 68 st. 1 tač. 6): **50k–500k for a preduzetnik** (500k–2M is the pravno-lice tier) + possible 6mo–2yr activity ban | Shop | [ZoT čl. 29; čl. 68 st. 1 tač. 6](https://www.paragraf.rs/propisi/zakon_o_trgovini.html) | Gap (no goods-receipt documents) | SW-9b (kalkulacija) |
| 17 | **KEP evidencija prometa — penalty CORRECTED.** Per prodajno mesto, based on isprave, „dostupna na prodajnom mestu“ (čl. 30 st. 6). Two distinct offences: **KEP kept improperly/incompletely** (čl. 67 st. 1 tač. 2) = fixed **100k pravno lice / 40k preduzetnik**; **not keeping the KEP at all** (čl. 68 st. 1 tač. 7) = **50k–500k preduzetnik** + possible activity ban. **Retail preduzetnik is bound unconditionally** — no paušal/wholesale exemption (PEP **čl. 2 st. 3**) | Shop | [ZoT čl. 30; čl. 67 st. 1 tač. 2; čl. 68 st. 1 tač. 7](https://www.paragraf.rs/propisi/zakon_o_trgovini.html) | **Re-stated 07.08.2026 — the module shipped 19.07.2026 (SW-9a/b/c); it keeps ONE book, not one per prodajno mesto.** `kep.rs` is the append-only value ledger — INSERT-only per PEP čl. 14, with the go-live reset as its one sanctioned wipe — `commands/kep.rs` ships thirteen admin-gated commands and all thirteen are registered in `lib.rs`'s invoke handler (`:240–252`), and **KEP** is a shell module of its own (`src/app/navigation.ts:55`, `src/app/kep/KepModule.tsx`). Čl. 30 st. 6's *„dostupna na prodajnom mestu“* is discharged offline from the till: `kep_export_book` renders the book and `KepModule` hands the written file to `PrintService.openForPrint`, with no cloud in the path. **The per-prodajno-mesto limb is NOT built.** `kep_entries` (v12) is partitioned by `book_year` alone — no prodajni-objekat column, and `kep_ledger` takes only a year — so the application keeps one book with one `redni_broj` sequence. That is correct for the single-outlet pilot and wrong for a second outlet, which čl. 30 would give a book of its own; `kalkulacije.prodajno_mesto` is a printed header field, not a partition key | SW-9a ✓; a second prodajno mesto would need a book of its own |
| 18 | **KEP mechanics — VERIFIED + CORRECTED.** 5-column obrazac (RB / datum dan-mesec / opis / zaduženje / razduženje); **zaduženje = retail value WITH PDV** (not nabavna); razduženje = daily fiscal promet; derived saldo. **Kalkulacija cene = FOURTEEN elements** (the „13“ was a miscount). **Nivelacija all in kolona 4** (downward = crveni storno −); **customer contract-rescission return in kolona 5** storno; popis višak→kol4, manjak→kol5 (four storno paths — a hard cause→column map). Entries by next day; sell-after-entry gate. **čl. 18 year-end lock = data-layer immutability (prevention, NOT tamper-detection).** 5-year retention floor. Governing bylaw PEP 99/2015+44/2018 still operative; no successor as of 07.2026 | Shop | [PEP čl. 3, 10–19](https://www.paragraf.rs/propisi/pravilnik_o_evidenciji_prometa.html) (live via [ZoT čl. 71 st. 2](https://www.paragraf.rs/propisi/zakon_o_trgovini.html)) | **Re-stated 07.08.2026 — the mechanics shipped 19.07.2026 (SW-9a/b/c) on the SW-8 printing stack.** Five-column obrazac and derived saldo in `kep.rs::list_ledger` / `kep_close::render_book_html`, kolona 2 rendered `dan.mesec`; **zaduženje at retail value with PDV** in `kep.rs::post_receipt_zaduzenje`, written inside the goods-receipt transaction so a receipt can never exist un-booked; razduženje from `post_daily_sales`. The **fourteen** kalkulacija elements are `kep_kalkulacija.rs::derive_kalkulacija`, persisted by `create_kalkulacija` and rendered by `render_kalkulacija_html` — the *„13“* miscount appears nowhere in the crate. The cause→column map is `kep_storno.rs::posting_for`: one exhaustive `match` over `StornoCause` returning kolona, sign and `kind`, **never overridable by a caller**, with `post_nivelacija` routing naviše and naniže into kolona 4 (down as a crveni storno) and moving the catalog price, `price_history` and the KEP delta in one transaction. The next-day rule is `kep.rs::kep_status`'s `overdue_sales_days` (T+1, oldest first), and the sell-after-entry gate is structural — the receipt zaduženje shares the inventory transaction. Čl. 18's year-end lock is `kep_close::ensure_year_open`, and it fences **every** production insert path (`kep.rs:84`, `:241`, `:358–359`; `kep_kalkulacija.rs:110`; `kep_storno.rs:210`, `:270`). The 5-year floor is `kep_close::retention_floor`, anchored on the later of the book year's end and `closed_at`. **Two things to keep straight rather than read as closed.** The čl. 18 lock is a **domain gate each writer calls, not a storage-layer trigger** — v18–v21 use `BEFORE UPDATE` triggers for immutability elsewhere and `kep_entries` has none, so a future writer that omits `ensure_year_open` is caught by review rather than by SQLite. And nothing sweeps the book: `kep_close::purge_eligible` only decorates a closure row in `list_closures`, and the KEP has no `RecordClass` on the shared retention table | SW-8 ✓, SW-9a/b/c ✓; the čl. 18 lock has no storage-layer trigger behind the domain gate |
| 19 | Annual popis (stocktake) + reconciliation at datum bilansa; computer-assisted allowed with **printed, signed** popisne liste; goods handlers excluded from commission (single person OK for micro/preduzetnik); izveštaj ≥60 days before FS deadline | Shop | [ZoRač čl. 20](https://www.mfin.gov.rs/upload/media/RjRvaT_6017f919eddde.docx); [PoP čl. 5, 6, 9 st. 3, 13](https://www.mfin.gov.rs//upload/media/MiN4yY_6018313a549f9.docx) | **Shipped 03.08.2026 (SW-16); reqs. 31/32/35 added 07.08.2026 — re-stated here as one current state rather than as a chain of corrections, with the dated disclosure kept in `docs/PROGRESS.md`.** Migrations **v20** (`popis_sessions`, `popis_lines`, `popis_signatures`, `popis_commission`) and **v21** (`plan_rada_odobrio`, `plan_rada_odobreno_at`, `odluka_doneta_at`), the six-state machine in `popis.rs`, the admin-gated commands in `commands/popis.rs`, the four documents in `popis_print.rs`, and Popis in the shell. The čl. 8 st. 5 blind count is enforced at the **query layer** and anchored on the `faza = 'a'` potpis, not on `status`; the čl. 20 st. 3 reconciliation is a gate on opening; the čl. 9 st. 2 perpetual shortcut needs a posted in-year popis; posting write-locks the documents. **Čl. 9 st. 3's *„uz štampanje“* is now discharged inside the application:** `popis_export_lista`, `popis_export_odluka` and `popis_export_plan_rada` write the popisne liste, the odluka o popisu i obrazovanju komisije and the plan rada into `exports/`, and `PopisModule` hands each written file to `PrintService.openForPrint` — the komisija signs the paper, and the exported file is not a signed isprava. The phase of the lista is **derived** from `book_quantities_released(status, faza_a_potpisana)` and never chosen by the screen — the frontend may narrow it to Faza A and can never ask for Faza B — and the čl. 8 st. 5 sheet is rendered by a code path that never names `knjigovodstvena_kolicina_milli` or `razlika_milli` at all, because blanking them would look identical from outside. Req. 32's header block (obveznik, PIB/MB, maloprodajni objekat, broj liste, datum i period) and the three per-stavka vrednost columns are on the exported lista. Req. 35: `popis_odobri_plan` records the PoP čl. 8 st. 2 approval against a name the operator supplies — a blank one is refused rather than filled in from the registered obveznik, because a server-side default would be an auto-approval wearing a default's clothes — and both generated documents state that the application holds the *odobrenje* and not the *sadržina* of the plan rada wherever `plan_rada_json` is empty. **Four limbs are NOT built and the shop must know it.** **(1)** Req. 32's **editable default template**: the column set is fixed in `popis_print.rs`, with no template, no setting and no operator control over the layout. **(2)** The **izveštaj o popisu** is **neither printed nor exported** — `popis_izvestaj` composes it on demand and returns it to the screen, and the printed, signed primerak is the obveznik's own to make and keep; the retention napomena, the čl. 47 register entry and the izveštaj's own upozorenje all say so and all name the three documents the program does write. **(3)** **`odluka_doneta_at` is written by no verb**: exporting a draft odluka is not *donošenje odluke*, so the column stays empty and the odluka's own header cell says the date is not recorded in the application and is to be entered on the printed primerak. **(4)** The čl. 2 st. 6 primerak is **not delivered** — the app computes the ten-day rok from the liste and says in the same breath that it sends the lista to nobody. The *document* is producible: since 07.08.2026 `popis_export_lista` takes a `lista` narrowing that writes **one** posebna popisna lista as a file of its own, every lista section carries its own signature block and starts on a page of its own, and `PopisModule` offers that action for each lista that has stavke. Until then the export bundled all six liste under a single trailing signature, so the primerak could not be assembled without either handing a third party the shop's whole counted inventory or tearing out an unsigned sheet — the delivery, and only the delivery, is what remains the obveznik's. **Req. 40, second limb:** the named popisivači, the `rukuje_imovinom` flag and the warn-never-block behaviour are all present and correct, but reusing that flag for **ZoRač čl. 10 st. 5** (control of računovodstvene isprave) has zero occurrences in the crate. And `perpetual_odluka_ref` is validated at opening and then never consumed: no code path shortens or skips the count, so čl. 9 st. 2's *„the exception excuses step 2) only“* is not modelled — the conservative direction, but the field buys a gate and no behaviour; its *usvojen* limb has no stored fact to check either, because nothing records the čl. 14 st. 2 odluka o usvajanju, and the module says that it does not record it rather than implying that it does. Three questions stay open, see [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §6: **R-5** (goods-handler exclusion *shodno* to a single-person popis — warn, never block), **R-6** (purely electronic potpis under čl. 9 st. 3 — print-and-sign is the default and the app says the recorded potpis does not replace the one on paper), **R-7** (no provision names popisne liste expressly for retention — 5 years on the isprave footing) | SW-16 ✓; liste, odluka and plan rada exported 07.08.2026 — still owed: req. 32's editable template, a print or export of the izveštaj, a verb that records `odluka_doneta_at`, and req. 40's ZoRač čl. 10 st. 5 limb |
| 20 | Retention: FS 20y; dnevnik/glavna knjiga 10y; pomoćne knjige 5y from closing; isprave 5y; payroll **trajno**; electronic archiving with unalterability + backup DB at second location | Shop | [ZoRač čl. 28](https://www.paragraf.rs/propisi/zakon_o_racunovodstvu.html) | **Re-stated 07.08.2026 — the go-live reset is guarded; the restore deliberately is not, and the general engine is still missing.** `reset_trading_data` (`commands/backup.rs:483`) forces a fresh snapshot before it touches anything (`create_backup`, `:493`), takes `retention::never_purge_row_counts` inside its own transaction (`:510`) and calls `retention::assert_never_purge_intact` immediately before the commit (`:590`) — so a future edit that adds a DELETE against a `never_purge` table aborts the whole reset instead of committing it — and writes a permanent `trading_data_reset` tombstone carrying `ROK_CUVANJA_PRAVNI_OSNOV` (`backup.rs:39`, *„ZoRač čl. 28 st. 4; ZPPPA čl. 114ž“*), the same constant the Podešavanja dialog prints. `work_time_entries`, `work_time_periods` and `retention_policies` are left standing by name. `retention.rs` is the shared class table itself. **Four limbs stay open, and the first is a decision rather than a gap.** **(1)** `restore_backup` (`:343`) is **not** fenced and cannot be — it swaps the database file, so the register returns as the snapshot holds it; the `pre_restore` safety copy is the protection on that path, the `backup_restored` event records the never-purge counts on **both** sides as evidence, and a count-based refusal could not tell a legitimately older backup from data loss. **(2)** A backup-prune path **does not exist in the crate**, so SW-3's *„exclude closed-year backups from rotation“* guards nothing yet. **(3)** There is still **no general upward-only `retain_until` engine over trading data** — the declared classes are the worktime, personnel, credential, access-log, čl. 47, cenovnik and popis ones, and the KEP carries its own floor in `kep_close::retention_floor` (row 21 states the same residual). **(4)** Čl. 28's *„backup baze podataka na drugoj lokaciji“* is `BackupSettings::backup_folder` — operator configuration the app writes into and never verifies is elsewhere. Backup encryption remains opt-in, per row 8 | SW-3 — reset guarded and tombstoned; still owed: the general `retain_until` engine over trading data, and row 8's default-on encryption |
| 21 | **Retention — mechanism CORRECTED 31.07.2026, see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §2 Q4.** ZPDV čl. 47 sets **no general period of its own** — it defers to zastarelost; the 10-year figure comes from **ZPPPA čl. 114ž** (absolute), 5y relative from čl. 114. čl. 47's own *"najmanje deset godina"* is **object-specific** (čl. 32 objekti i ulaganja), running *"po isteku kalendarske godine"* from first use / completion. **10 years is a floor, NOT a wall-clock ceiling** — zastoj periods are excluded (čl. 114z st. 2), so `retain_until` must be extendable upward. **PDV status does NOT gate the floor** — it only changes which statute supplies it: a non-PDV shop on dvojno knjigovodstvo still gets 10y for dnevnik/glavna knjiga under **ZoRač čl. 28 st. 4** | Shop | [ZPDV čl. 47](https://www.paragraf.rs/propisi/zakon_o_porezu_na_dodatu_vrednost.html); [ZPPPA čl. 114, 114ž, 114z st. 2](https://www.paragraf.rs/propisi/zakon_o_poreskom_postupku_i_poreskoj_administraciji.html); [ZoRač čl. 28 st. 4](https://www.paragraf.rs/propisi/zakon_o_racunovodstvu.html) | **Partial — onboarding half shipped 31.07.2026 (SW-15)**: `pdv_obveznik` is captured in `settings.rs::ShopProfile` and, as verified, does **not** gate anything; the go-live reset quotes the unconditional 10-year floor in its tombstone (`backup.rs`). **Still open:** a general upward-only `retain_until` engine over trading data (the KEP book has its own 5-year floor in `kep_close.rs::retention_floor`) | SW-3; onboarding retention profile (SW-15) |
| 22 | **Reklamacije — VERIFIED 19.07.2026, see [ZZP-REKLAMACIJE-VERIFIED-RULES.md](ZZP-REKLAMACIJE-VERIFIED-RULES.md).** Receive the complaint; issue a potvrda o prijemu carrying a sequential register number „bez odlaganja“ (synchronous, not a timer); answer ≤8 calendar days from **prijem**; resolve ≤15 days / ≤30 for **tehnička roba** (defined, čl. 5 t. 41) + **nameštaj** (undefined) from **podnošenje**; one extension only with recorded consumer consent; electronic evidencija with the 11 „naročito“ fields (čl. 55/63 st. 8, identical both regimes); retention ≥2y **from filing** (floor, not cap); ZZPL legal-obligation basis (not consent). **Register content is identical across regimes — the differences (below) are the clock, the no-fee ban, the express warning, and the penalty amounts.** New law = **ZZP 35/2026** (real gazette, publ. 23.04.2026; **in force 1 May 2026**; **application 2026-08-01 — RESOLVED 31.07.2026**: čl. 220 defers application three months from entry into force and čl. 219 repeals 88/2021 only on početak primene, so 1 Aug is right on the merits, not merely as a safe default). Regime keyed off the complaint's **filing date**, persisted at creation (čl. 215 transitional) | Shop | [ZZP 35/2026 čl. 63](https://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/14_saziv/1317-26-lat..pdf) / [ZZP 88/2021 čl. 55](https://www.paragraf.rs/propisi/zakon_o_zastiti_potrosaca.html) | **Shipped 31.07.2026 (SW-7)** — `reklamacije.rs` + `commands/reklamacije.rs` + `reklamacije_docs.rs`, admin-gated end to end. Intake allocates the sequential `register_number` (`MAX+1` inside the intake transaction, UNIQUE-guarded) and stamps `datum_izdavanja_potvrde` **synchronously**, with **no due-date computed for the potvrda** („bez odlaganja“ is not a timer); `reklamacija_export_potvrda` renders it. `compute_deadlines` derives `answer_due = filed + 8` and `resolution_due = filed + 15/30` per read — never stored — with the 30-day track gated behind an operator flag that defaults to the safe 15-day one. One extension only, requiring a consented new date, and the tighter of it and the statutory clock wins. All 11 „naročito“ fields are present: 1–6 as columns on `reklamacije`, 7–11 as structured `reklamacija_events` rows (`answer_given` / `consumer_received_answer` / `consumer_responded` / `extension_granted` / `resolved`) carrying `event_date`, `consumer_consent` and `detail_json`. `purge_eligible` flips at `filed_at + 730d` as **eligibility only** — nothing auto-deletes and no „must delete by“ date is shown. **Still open:** the register is role-gated but **not access-logged** — `commands/reklamacije.rs` writes no `audit_events` row, while `personnel.rs`, `breaches.rs` and `retention.rs` do. The ZZP memo §3 asks for both halves; note that SW-10's audit log shipped 02.08.2026 as a **prudential** control, not a logging duty, so wiring reklamacije into it is a design gap to close, not a statutory breach to remedy | SW-7; wire reklamacije reads into the SW-10 audit log |
| 23 | **Clock mechanics differ by regime (the 3-day consumer window + silence=„nije saglasan“ are NOT new — both existed in 88/2021).** OLD (88/2021 čl. 55 st. 10): resolution clock **restarts from zero** („prekida se … počinje da teče iznova“) when the trader receives the consumer's response; no warning duty. NEW (35/2026 čl. 63 st. 10): resolution clock **suspends/resumes** („zastaje … nastavlja da teče“), preserving elapsed days, AND the trader must **expressly** warn the consumer of the 3-day duty + consequences + zastoj. Answer clock (8d) is never suspended. NEW-only: **no fee for utvrđivanje nesaobraznosti** (čl. 63 st. 3) | Shop | [ZZP 35/2026 čl. 63 st. 10](https://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/14_saziv/1317-26-lat..pdf) vs [88/2021 čl. 55 st. 10](https://www.paragraf.rs/propisi/zakon_o_zastiti_potrosaca.html) | **Shipped (SW-7)** — `compute_deadlines` is two code paths keyed on the `regime` frozen on the record at intake and never recomputed: OLD restarts to a fresh full 15/30 from `consumer_responded`, NEW resumes at `base + (responded − received)`. The 8-day answer clock is derived independently and never pauses. Silence past `received + 3d` derives `impasse` and withholds a resolution date rather than silently extending it. `log_answer` **gates** the NEW-regime answer on all three express-warning parts being non-empty (čl. 63 st. 10) and does **not** impose them on OLD-regime answers. The **čl. 63 st. 3 no-fee ban shipped 02.08.2026** as *warn-and-attest*, not as a till-side block: no `sale_items` row references a reklamacija and there is no chargeable-service concept, so a charge for utvrđivanje nesaobraznosti is indistinguishable from lawful revenue and the software must not claim to detect one. What it does instead — `reklamacije.rs::ensure_no_fee_attested` refuses both `log_answer` and `resolve_reklamacija` for a NEW-regime record until `no_fee_attested` is set (v16), the gate is idempotent so the operator confirms once, the prohibition renders standingly in the module, on the per-record potvrda and on the static prodajno-mesto notice, and OLD-regime records are never gated, prompted or informed. Resolve is gated as well as the answer **because `resolve_reklamacija` requires no prior answer** — the on-the-spot path would otherwise escape it | SW-7 |
| 22a | **Reklamacija penalty — LOWER fixed tier, NOT 300k–2M, and no zaštitna mera.** Preduzetnik (the pilot): **30.000 din** old (88/2021 čl. 188 st. 3) → **100.000 din** new (35/2026 čl. 210 st. 1 tač. 24, st. 3) — ~4× rise at cutover, so penalty figures are regime-versioned | Shop | [88/2021 čl. 188](https://www.paragraf.rs/propisi/zakon_o_zastiti_potrosaca.html) / [35/2026 čl. 210](https://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/14_saziv/1317-26-lat..pdf) | **Copy shipped 31.07.2026 (SW-7)** — `legal.rs::reklamacija_breach` is the only place these amounts exist: tier-resolved from `ShopProfile::pravna_forma` and versioned by the record's own frozen regime, so a complaint filed pre-cutover keeps the 88/2021 figure forever. It rides on `ReklamacijaView.notice`, the module renders it **only on an overdue record**, and an unanswered pravna forma yields **no figure at all** rather than a plausible one. Tests pin both tiers, both regimes, the absence of any zaštitna mera and the unreachability of „privredni prestup“ | SW-7 (advisory, regime-versioned) |
| 24 | **Sniženje regime — CORRECTED 17.07.2026, see [ZOT-36-37-VERIFIED-RULES.md](ZOT-36-37-VERIFIED-RULES.md).** Display BOTH snižena + prethodna cena at the prodajno mesto (st. 2 — **in force since 2019, NOT new**; only the *definition* st. 3–5 is new on 1 May 2026). Prethodna cena = lowest **offered** („nudio“) price in the 30 days before campaign start (st. 3; perishable carve-out is from the *definition*, **not** from the display duty). Items <30 days in assortment → window „ne kraćem od 15 dana“ = a **window-length floor, NOT a ban on discounting** (st. 4). Progressive/uninterrupted → MIN(**applied**, „primenjivao“) over the 30 days **before campaign start**, **snapshotted and frozen** (st. 5) — **NOT "the price at the first reduction"** (that earlier claim was wrong). Seasonal: ≤2×/yr, start ∈ 25.12–10.01 ∪ 1–15.07, ≤60d **per instance** (st. 8–9 — **2019 rules, merely renumbered**). Akcijska ≤31d (st. 10); ≤3d akcija **substitutes a mandatory %** (st. 11). Promotivna prodaja ≤60d (čl. 36 st. 9, cap NEW 2026) and **never** carries a prethodna cena. Penalty: **fixed 100.000 pravno lice / 40.000 preduzetnik / 10.000 odgovorno lice; no zaštitne mere** | Shop | [ZoT čl. 37 st. 1–11; čl. 36 st. 2, 9; čl. 67 st. 1 tač. 8](https://www.paragraf.rs/propisi/zakon_o_trgovini.html) | **Re-stated 07.08.2026 — the price-history and sniženje engine shipped 17.07.2026 (SW-6a/b/c).** `price_history.rs` is the append-only **offered-price** log this row asks for: INSERT-only, with a **NULL `price_minor` as an explicit offering gap**, so returning seasonal stock is not read as a new arrival — the highest-risk trap named in the build spec — and `compute_prethodna_cena` answers the čl. 37 st. 3–4 question with a typed `IncomputableReason` (`TooNewInAssortment` / `NotOfferedInWindow` / `NoHistory`) and a `truncated` flag instead of a plausible number. `campaigns.rs` carries the closed four-type enum (`TYPE_RASPRODAJA` / `TYPE_SEZONSKO` / `TYPE_AKCIJSKA` / `TYPE_PROMOTIVNA`), `validate_shape` + `validate_against_db` for the window, 2×/yr, ≤60d, ≤31d and promotivna rules, and `snapshot_anchors`, which freezes the st. 5 anchor **once** at activation. `campaign_evidence.rs` renders all three artefacts — the walk-away inspector record (`render_evidence_html`), the shelf labels (`render_labels_html`) and the correction report — and the st. 2 display duty **is** what the labels are: the struck-through prethodna beside the nova cena, with the ≤3-day akcija's `-N%` substitution (st. 11) and the promotivna *Uvodna cena / Redovna cena po isteku* pair as the two deliberate exceptions. The three exports are wired in `lib.rs:220–223`. **Two limbs are not built.** The **penalty copy does not exist**: `legal.rs` carries no sniženje notice, so the fixed 40.000 preduzetnik of čl. 67 st. 1 tač. 8 is stated in this row and rendered **nowhere in the application** — the safe direction, and still an unshipped limb. And the **5-year retention is discharged by absence rather than by a class**: `retention::RecordClass` has no price-history or campaign variant, nothing in the crate deletes a `price_history` row, and the single path that clears the table is the go-live reset, which re-seeds it from the surviving catalog | SW-6a/b/c ✓; still owed: the čl. 67 st. 1 tač. 8 notice in `legal.rs` and a declared retention class for the offered-price log |
| 25 | **Cenovnik — VERIFIED + CORRECTED 01.08.2026, see [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2b.** Machine-readable cenovnik per prodajni objekat *"na svojoj internet stranici"*, real-time updates, scraping enabled, open-data-portal account. **Penalty tiers CORRECTED — the "200k/100k/50k" slash-list is three different subjects, and the pilot's number is the middle one: preduzetnik fixed 100.000 (čl. 210 st. 3)**; 200.000 is *pravno lice* (st. 1) and 50.000 is *odgovorno lice u pravnom licu* (st. 2), **a tier that does not exist for a preduzetnik**. Halved to 50.000 on payment within 8 days of a prekršajni nalog (ZoP čl. 173 st. 1). **Also corrected: čl. 210 is NOT in the čl. 220 carve-out**, so the *fine* is not live from 1 May — only čl. 4 st. 1 and čl. 6 are. **Scope splits:** SETTLED NEGATIVE — there is **no** "trader who has a website" carve-out in st. 2 (the drafters used *"Trgovac koji objavi…"* three times in st. 4/5/6 and not in st. 2), so the pilot may not rely on one; **UNRESOLVED POSITIVE** — whether a website-less trader must *create* a site (st. 2 says *"na **svojoj**"*, a possessive presupposing an existing site, and no ZZP provision obliges a trader to have one; extending a prekršaj to an unwritten duty runs into lex certa, ZoP čl. 3). **Never tell the pilot it is in breach today** | Shop | [ZZP26 čl. 6, čl. 210 st. 3, čl. 220](https://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/14_saziv/1317-26-lat..pdf) | **SW-12 shipped 02.08.2026** — export, republish-on-write, archive, till guard and a local publish target; **still open:** the hosted endpoint (F-13) and the čl. 6 st. 6 open-data-portal account, whose format the missing bylaw has not defined. Bylaw pending (§6 item 12), no-website scope UNRESOLVED (§6 item 4) | SW-12 |
| 26 | Shelf price display (prodajna + jedinična cena), clear and legible; no customer-display duty survives e-fiscalization | Shop | [ZoT čl. 35](https://www.paragraf.rs/propisi/zakon_o_trgovini.html); ZZP26 čl. 6 st. 1 | N/A (shelf duty) | Optional label-printing feature only |
| 27 | **Deklaracija + mašinski čitljiva oznaka — VERIFIED + CORRECTED 31.07.2026, see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §2 Q3.** **Creating** the deklaracija and applying the GTIN/QR mark is the duty of the **proizvođač, odnosno uvoznik** (st. 2) — **not** of a reselling retailer. The shop's exposure is a **selling** offence, so the compliance point is **goods receipt**, not checkout. Three stavovi bind the trgovac directly: **st. 3** (may not alter/remove declaration data), **st. 4** (conspicuous display at the prodajno mesto, one of three ways), and — **the one genuinely new 2026 retailer duty** — **st. 5 (daljinska trgovina)**, strengthened by 35/2026 from "učine dostupnim podatke" to "**istaknu deklaraciju sa podacima iz stava 1** … i te podatke učine dostupnim potrošaču pre kupovine". GTIN duty (st. 8) is **old law, applicable since end-January 2020**; 35/2026 added exactly one word ("identifikacionom") and renumbered old st. 9–10 → st. 11–12. New st. 9–10 (jedinstveni šifarnik robe) bind the **Ministarstvo**, not the shop; no pravilnik traced as of 31.07.2026 (negative evidence — mtt.gov.rs unreachable). **Penalties are the preduzetnik tier:** defective deklaracija (**čl. 67 st. 1 tač. 6** — was tač. 4 before 1.5.2026, a renumbering trap) = **fixed 40.000** (st. 3), no zaštitne mere; **no** deklaracija (čl. 68 st. 1 tač. 9) = **50.000–500.000** (st. 3) **+ possible zabrana vršenja delatnosti 6 mo–2 yrs (st. 6)** — the sanction that would actually close the shop. Attach **no** fine figure to a bare missing GTIN (§6 item 16) | Shop | [ZoT čl. 34 st. 2–5, 8; čl. 67 st. 1 tač. 6 + st. 3; čl. 68 st. 1 tač. 9 + st. 3, st. 6; čl. 69a](https://www.paragraf.rs/propisi/zakon_o_trgovini.html) | **Shipped 31.07.2026 (SW-11c)** — `manufacturer_name` / `importer_name` / `country_of_origin` / `barcode_kind` + the deliberately un-wired `official_goods_code` (stored and operator-editable, but no jedinstveni-šifarnik sync logic until the bylaw lands) on `products` (v15); `catalog.rs::validate_declaration` makes the two st. 1 identification fields it stores — `manufacturer_name` and `country_of_origin` (the literal "EU" accepted per st. 7) — **mandatory only when `distance_selling` is `Some(true)`** (čl. 34 st. 5) and advisory otherwise; `importer_name` stays optional because st. 1 requires the uvoznik only za robu iz uvoza; goods receipt raises `declaration_warnings` (`inventory.rs`), the „Artikli bez podataka deklaracije“ report is `catalog_declaration_gaps`, and every fine figure comes from `legal.rs::declaration_missing` / `declaration_defective` | SW-11c; jedinstveni šifarnik bylaw pending — design allowance (separate nullable column, no sync logic) |
| 28 | **AML gotovinski prag — VERIFIED + penalty CORRECTED 31.07.2026, see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §2 Q1.** Encode the **ban** side with an **inclusive** operator: receiving cash **≥ EUR 10.000** dinar countervalue is already unlawful (*"u iznosu od 10.000 evra **ili više**"*) — a `>` comparison is a compliance bug. Trigger is the **cash tendered**, not the invoice total (5.000 € cash + 10.000 € card is not a breach on the face of st. 1). Covers one or more linked cash transactions **or one or more contracts within one year** (that limb + the mandatory-bank-transfer command + the st. 3 carve-out are **new in 94/2024, in force 06.12.2024**). Binds **every trader** — čl. 46 says "Lice", not "Obveznik" — so the shop is caught even though it is **not** an obveznik under čl. 4 (hence **no** čl. 47 15.000 € reporting duty, no CDD). Supervisor: **tržišna inspekcija** (čl. 110 st. 6). **Penalty: a preduzetnik CANNOT commit a privredni prestup** — its exposure is a **prekršaj 100.000–300.000 RSD (čl. 120 st. 2)**, and 300.000 is a *base* maximum, not a ceiling (uplift čl. 120 st. 8 → ZoP čl. 39 st. 4). Pravno lice: privredni prestup 100.000–2.000.000 (**čl. 118 st. 1 tač. 43**) + odgovorno lice 10.000–150.000 (čl. 118 st. 2) | Shop | [AML čl. 46 st. 1–3](https://www.paragraf.rs/propisi/zakon_o_sprecavanju_pranja_novca_i_finansiranja_terorizma.html); čl. 110 st. 6; čl. 118 st. 1 tač. 43; čl. 120 st. 2, st. 8 | **Shipped 31.07.2026 (SW-11a)** — `aml.rs::assess_cash_payment` (inclusive `>=`, subject is the **cash line of the tender**), rate from `nbs_rate.rs` (zvanični srednji kurs, manual fallback, staleness never silently passes), notice text tier-resolved in `legal.rs::aml_cash_cap`; asked by `sales_assess_cash_payment` as the cash is entered and re-evaluated + persisted on the sale by `sales_complete`; the statute's own remedy ships as the `bank_transfer` tender | SW-11a |
| 29 | **Polog pazara — VERIFIED 31.07.2026 (the 7-day rule SURVIVED an adversarial attack), see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §2 Q2.** Deposit **all** dinars received in cash *"po bilo kom osnovu"* — not merely customer takings — onto the business account within **seven radni dani**. Clock runs from **receipt of the cash**, not from shift close or the Z-report; partial deposits are lawful. **Bylaw-level relief (Pravilnik 77/2011 čl. 5 st. 2) — NARROWED 01.08.2026:** cash withdrawn from the entity's own account is **not** "gotov novac" for this duty, **but only where the payout was made per čl. 2 st. 2** (against original documentation submitted to the bank na uvid i overu) **or čl. 2 st. 3** (the ≤150.000 RSD/day undocumented lane). The app therefore excludes a `bank_withdrawal` **only when the operator has recorded that assertion** (`cash_movements.documented_per_pravilnik = 1`, migration v16); an unasserted withdrawal stays **in** the base. A blanket exclusion — which is what shipped first — errs in the **unsafe** direction: it shrinks the čl. 3 st. 1 base and can hand the owner a false „izmireno“. Without any exclusion at all the app would instead age the owner's change float as undeposited pazar. The rival *"istog dana, a najkasnije narednog radnog dana"* rule was tested and is the **repealed** predecessor (died 17.05.2011). **No statutory blagajnički maksimum exists.** Fines (all prekršaj; act has no privredni prestup): 50k–2M pravno lice (**čl. 7 st. 1 tač. 2**) / **5.000–150.000 odgovorno lice (čl. 7 st. 2)** / 10k–500k preduzetnik (čl. 7 st. 3). Supervisor: **Poreska uprava** (čl. 6). Do **not** abbreviate this act "ZOP" — that conventionally denotes the repealed *Zakon o platnom prometu* | Shop | [Zakon o obavljanju plaćanja pravnih lica, preduzetnika i fizičkih lica koja ne obavljaju delatnost, Sl. glasnik RS 68/2015, čl. 3 st. 1, čl. 6, čl. 7](https://www.paragraf.rs/propisi/zakon_o_obavljanju_placanja_pravnih_lica_preduzetnika_i_fizickih_lica_koja_ne_obavljaju_delatnost.html); [Pravilnik 77/2011 čl. 2, čl. 5](https://www.paragraf.rs/propisi/pravilnik_o_uslovima_i_nacinu_placanja_u_gotovom_novcu_u_dinarima_za_pravna_lica_i_za_fizicka_lica.html) | **Shipped 31.07.2026 (SW-11b)** — `cash_deposit.rs`: `add_working_days` over a seeded, operator-editable `non_working_days` table (Saturday configurable), `build_buckets` ages **all** cash received „po bilo kom osnovu“ from **receipt** with oldest-bucket-first drawdown for partial pologa, deadline rendered as a **date**, and `bank_withdrawal` excluded from the subject base (Pravilnik 77/2011 čl. 5 st. 2); notice text from `legal.rs::cash_deposit_duty`. **Advisory only — no hard block** | SW-11b |
| 30 | Retail is excluded from mandatory e-invoicing **except** (since tax periods after 31 Mar 2026): sales to korporacijska kartica holders, and to public-sector buyers who request within 7 days — then e-faktura via SEF in addition to (never before) the fiscal receipt; refund/re-document mechanics per [Pravilnik o PDV čl. 198a](https://www.mfin.gov.rs/sr/propisi-1/pravilnik-o-pdv-1) | Shop | [ZEF čl. 3 st. 2 tač. 1, st. 3](https://www.parlament.gov.rs/upload/archive/files/lat/pdf/zakoni/14_saziv/2166-25-LAT..pdf) | N/A for software (ESIR-side flow); onboarding awareness | SW-15 (record ESIR receipt no. + buyer PIB against sale); F-8 |
| 31 | If fakture are ever issued outside fiscalization: 10 mandatory elements incl. sequential numbering, supply date, base/rate/VAT | Shop | [ZPDV čl. 42 st. 5](https://www.paragraf.rs/propisi/clanovi/clan-42-zakona-o-pdv.html) (renumbered by 109/2025) | N/A (VantumPOS issues no fakture) | Keep out of scope; schema allowance only |
| 32 | **Archive law — scope CORRECTED 31.07.2026, see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §1 rows 5 & 12.** For a **pravno lice**: arhivska knjiga, prepis to archive by 30 April, lista kategorija **with the archive's saglasnost**; fines 50k–2M. **The "no destruction without prior written archive approval" duty does NOT bind a private company** — ZAG **čl. 16 st. 2** confines it to state/TA-LSG organs, ustanove, javna preduzeća and imaoci javnih ovlašćenja. For a **preduzetnik**: čl. 65 has **no preduzetnik tier**, so no enforceable fine — but "exempt" is overstated, because only čl. 9 **st. 2** carries the *"osim fizičkih lica"* carve-out; **čl. 9 st. 1 (savesno čuvanje u sređenom i bezbednom stanju) and st. 4 have none**. No penalty ≠ no duty — never tell a preduzetnik archive law does not apply to him | Shop if d.o.o.; **Actaer itself** | [ZAG čl. 9 st. 1, st. 2 tač. 6–7, st. 4, čl. 14, čl. 16 st. 2, čl. 65](https://www.paragraf.rs/propisi/zakon-o-arhivskoj-gradji-i-arhivskoj-delatnosti.html) | N/A software; founder duties | F-9; capture shop legal form (SW-15); reset warning (SW-3) |
| 33 | e-Otpremnica: from 1 Jan 2026 public sector + excise flows; from **1 Oct 2027** all private-to-private incl. recipient duties (confirm physical receipt ≤3 working days; accept/reject by sending e-prijemnica ≤8 days from confirmation); fines 200k–2M RSD; error grace to 30 Jun 2026 | Shop (VAT payers; non-VAT only for excise) | [ZEO čl. 2, 7 st. 4–5, 8 st. 1, 13, 16](https://www.paragraf.rs/propisi/zakon-o-elektronskim-otpremnicama.html) | N/A until Oct 2027 (boutique, non-excise) | SW-18 roadmap slot |
| 34 | *Flagged, not adversarially verified:* textile fibre-composition labeling (Pravilnik 1/2014 / EU 1007/2011). **Nesaobraznost presumption CORRECTED + VERIFIED 19.07.2026:** the citation „čl. 56“ was WRONG (čl. 56 is the *remedies* article). It is **35/2026 čl. 59 st. 3** — presumption window **1 year from isporuka** (delivery), burden on trgovac — vs OLD **88/2021 čl. 52 st. 2**, **6 months from prelazak rizika**. Drives an advisory flag, not a hard rule. See [ZZP-REKLAMACIJE-VERIFIED-RULES.md](ZZP-REKLAMACIJE-VERIFIED-RULES.md) §5. **The seasonal-sale rules formerly listed here are now VERIFIED and moved into row 24** — they are real, but were mis-attributed (čl. 37 st. 8–9, not „čl. 36–37“), mis-dated (2019, not new), and the „max 60 days“ claim conflated two distinct caps. See [ZOT-36-37-VERIFIED-RULES.md](ZOT-36-37-VERIFIED-RULES.md) §1 | Shop | See §6 | Gap-adjacent | SW-20 |

---

## 3. Software change list (prioritized)

**P0 — before any pilot ships:**

| ID | Build | Legal driver | Effort |
|---|---|---|---|
| SW-1 | "OVO NIJE FISKALNI RAČUN" banner (prominent, non-removable, ≥2× line-item font) on the post-sale dialog and on every current/future export that itemizes a sale; hard design rule: no QR, no PIB/brojač block, no "FISKALNI RAČUN" framing anywhere | PVFR čl. 2 st. 8–10; ZF čl. 15 st. 1 tač. 5 defense | S |
| SW-2 | Encrypt local backup files (key handling documented); this is the first-named čl. 50 measure and the čl. 53 st. 3 tač. 1 breach-notification safe-harbor | ZZPL čl. 50 st. 2 tač. 1, čl. 53 | M |
| SW-3 | Retention guards: go-live reset and typed-confirmation restore must (a) force a fresh export/backup snapshot first, (b) display a warning quoting the **10-year** retention horizon (ZoRač čl. 28 st. 4; ZPDV čl. 47 → ZPPPA čl. 114ž), (c) write a permanent tombstone/log record of who/when/what was wiped; exclude closed-year backups from rotation/pruning. **CORRECTED 31.07.2026:** the "archive-law **destruction-approval** duty for d.o.o. shops" **does not exist** — ZAG čl. 16 st. 2 is public-sector-only; a private pravno lice's real duties are the lista kategorija sa saglasnošću, the arhivska knjiga and the 30 April prepis. Legal driver **ZPPPA čl. 175b was wrong** (that is the *criminal* evasion-software offence, not a retention rule) — the tax-record retention penalty is **ZPPPA čl. 178b** (preduzetnik 50.000–500.000); ZPDV čl. 60/60a has ceased to be in force | ZoRač čl. 28; ZPDV čl. 47 + ZPPPA čl. 114ž, čl. 178b; ZAG čl. 9/65 | M |
| SW-4 | Anti-evasion invariants audit: verify finalized sales cannot be edited in place or deleted tracelessly; voids/returns always remain in the ledger; document the result in the compliance posture note (F-7) | ZPPPA čl. 175b | S |
| SW-5 | Sale-completion nudge "Izdaj fiskalni račun na ESIR-u" + optional per-sale field for the ESIR fiscal receipt number (doubles as reklamacija proof-of-purchase link and B2B PIB reconciliation) | ZF čl. 4 st. 2 posture; ZZP26 čl. 63 st. 5 | S |

**P1 — during pilot (ordered):**

| ID | Build | Legal driver | Effort |
|---|---|---|---|
| SW-6 | **Price history + sniženje engine — scope CORRECTED 17.07.2026; full encodable spec in [ZOT-36-37-VERIFIED-RULES.md](ZOT-36-37-VERIFIED-RULES.md).** Append-only **offered-price** log keyed `(sku, prodajno_mesto)` storing **validity intervals** so offering gaps are explicit (returning seasonal goods are NOT new items — the highest-risk trap, and it is exactly the pilot's July stock). Cannot be derived from receipts: „nudio“ ≠ sold. Campaign entity over the closed 4-type enum {rasprodaja, sezonsko_sniženje, akcijska_prodaja, promotivna_prodaja} with an anchor **snapshotted once and frozen**. Hard validations: seasonal window/2×yr/≤60d per instance, akcijska ≤31d, ≤3d akcija must show two prices OR a %, promotivna ≤60d + no-prior-history, rasprodaja statutory grounds + no-restocking check. Retention **5y** — never prune on the 30-day window. Print-on-demand + walk-away inspector export | ZoT čl. 37 st. 1–11; čl. 36 st. 2, 9 (st. 3–5 in force 1 May 2026; the rest since 2019) | **L** (was M — verification enlarged it) |
| SW-7 | **✅ SHIPPED 31.07.2026, completed 02.08.2026 — one residual, named below** — `reklamacije.rs` (deadline engine + register), `commands/reklamacije.rs` (admin-gated command layer), `reklamacije_docs.rs` (potvrda + prodajno-mesto obaveštenje), `ReklamacijeModule.tsx`. Regime frozen at intake from `filed_at` vs `CUTOVER_DATE`; deadlines derived per read, never stored; one consented extension, tighter date wins; all 11 evidencija fields as columns + structured events; `purge_eligible` at `filed_at + 730d` as eligibility only; penalty copy tier-resolved in `legal.rs::reklamacija_breach` (row 22a); čl. 63 st. 3 no-fee ban as warn-and-attest, gating both the answer and the resolve (row 23). **Residual:** the register is admin-gated but reads are not access-logged (row 22). SW-10's audit log shipped 02.08.2026 and reklamacije was never wired into it, so this is now a wiring gap rather than a dependency. Build spec below is the original 16.07.2026 text; the „switch mechanics on 2 Aug 2026“ clause in it was superseded — application is **1 Aug 2026** (row 22), and `CUTOVER_DATE` is `2026-08-01`. — Reklamacije module: intake (name, date, goods, defect description, zahtev/remedy choice), sequential register number, auto potvrda o prijemu, 8-day answer timer, 15/30-day resolution timer (tehnička roba/nameštaj flag), one-shot extension with recorded consent, suspend/resume clock + mandatory 3-day-warning text in the response template, all čl. 63 st. 8 evidencija fields, ≥2-year retention then purge, role-restricted PII access | ZZP26 čl. 63 | L |
| SW-8 | Printing/PDF stack (hard prerequisite for KEP print-on-demand, popis lists, potvrda o prijemu; app currently prints nothing) | PEP čl. 10; PoP čl. 9 st. 3 | M |
| SW-9 | KEP module: one book per prodajni objekat; 5-column obrazac (redni broj, datum, opis promene w/ document name-no-date + supplier for purchases, zaduženje, razduženje); kalkulacija cene document on goods receipt (all 13 PEP čl. 15 st. 5 tač. 1 elements); nivelacija (increase = like purchase; decrease = crveni storno); auto daily razduženje from the sales ledger; unposted-prior-day warning (next-day rule) + received-goods-before-KEP-entry warning; year-end zaključenje freezing the year append-only; instant print/PDF | ZoT čl. 29–30; PEP čl. 10–11, 15, 17–18 | L |
| SW-10 | **✅ SHIPPED 02.08.2026 — and it ships as what it is: a PRUDENTIAL control the shop keeps by choice, never a logging duty.** Migration **v18** carries `support_sessions` and `audit_events`; the latter has a closed `CHECK` on the čl. 48 st. 1 verbs (`unos / menjanje / uvid / otkrivanje / uporedjivanje / brisanje`), an actor **FK to `users`** rather than a name column, closed enums for `reason_code`, `recipient` and `object_type`, `prev_hash` + `hash` NOT NULL, **no `updated_at`**, and a `BEFORE UPDATE` trigger that refuses an edit at the storage layer. **`audit.rs`** is pure: `chain_hash` / `verify_chain_anchored` (SHA-256, tail truncation reported, survivable across a purge) plus `reject_forbidden_content`, the **write-boundary** filter req. 4 asks for — the object id must match a whitelisted opaque-id grammar, so a JMBG, a PAN, an address, a phone, a free-text note or a search string cannot enter the table even from a future caller that means well. **`commands/audit.rs`** ships the čl. 46 nalog (`support_grant_access` — admin-gated, explicit obim and expiry, hard-bounded at 24 h — plus `request_access` gated by the *nalog* and not by a role, `end_session`, `active_session`) and the log's only three verbs: `record_audit` (the single door), `search` (admin-only; reading is deliberately not logged) and `export_csv`, the izvod modelled on čl. 48 st. 4 and handed over on the čl. 49 osnov, which renders offline from the till and logs its own `otkrivanje`. Retention is the `access_log` class in the shared table — **2-year default, upward-only, never `trajno`** — and the purge takes the head of the chain and records an anchor so the survivors still verify. Frontend: `src/app/privacy/` (Privatnost), with **Daljinska podrška first** so the module's legally weightiest surface is its first one; the copy states that **za povredu člana 50 nije propisan prekršaj** and names the opomena and the obavezujući nalog that *are* the consequences. Req. 9 closed: the čl. 23 notice now carries the evidencija-pristupa section. **RECLASSIFIED 01.08.2026 — PRUDENTIAL / EVIDENTIAL, NOT a legal duty. See [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2a.** **No Serbian provision requires a private retail controller to log access.** Three independent legs: (i) čl. 50 st. 2's candidate-measure list does not include logging, and st. 5 is an *instruction-control* duty, not record-keeping; (ii) the two express logging duties — **čl. 48** (beleženje radnji obrade) and **čl. 51 st. 2 tač. 6–7** (kontrola prenosa / kontrola unosa) — are both confined to a *nadležni organ … u posebne svrhe*, and čl. 50 st. 6 confirms the boundary from the other side. Expressio unius: the legislature wrote a logging duty **twice** and confined both to competent authorities; (iii) **čl. 50 is absent from čl. 95** — the entire penal chapter — so **no prekršaj attaches**. Keep the feature, relabel it. Honest citation: čl. 5 st. 2 + **čl. 41 st. 1** (accountability — the controller must be *able to demonstrate*) and čl. 50 st. 1, with čl. 48 st. 2 / čl. 51 st. 2 t. 7 as the **design template** borrowed from the law-enforcement regime. **čl. 46 stays operative** — it is the penalised instruction-only rule and the real hook for remote support. Register must say *"no prekršaj is prescribed"*, **never** *"no consequence"* — the Poverenik's corrective powers (opomena, nalog) are the enforcement route against an unpenalised provision | ZZPL čl. 41 st. 1, čl. 46, čl. 50 st. 1; čl. 48 / čl. 51 as template | M |
| SW-11 | **✅ SHIPPED 31.07.2026** — (a) `aml.rs` + `nbs_rate.rs` + `legal.rs`, surfaced by `sales_assess_cash_payment` and persisted by `sales_complete`, with the `bank_transfer` tender as the statute's own remedy; (b) `cash_deposit.rs` (working-day arithmetic, seeded `non_working_days`, FIFO buckets, and — **narrowed 01.08.2026, migration v16** — only a `bank_withdrawal` the operator has asserted as `documented_per_pravilnik` leaves the subject base) — advisory only; (c) declaration columns on `products` (v15) with `catalog.rs::validate_declaration` mandatory **iff `distance_selling`**, `catalog_declaration_gaps`, and goods-receipt warnings in `inventory.rs`. **Cash & product compliance trio — scope RESHAPED 31.07.2026; full encodable spec in [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §3.** (a) Warn when the **cash line of the tender** (never the grand total) reaches **`>=` EUR 10.000** at the **NBS zvanični srednji kurs for the transaction date, persisted on the sale** (replaces "configurable rate"); ship the statute's own remedy — an "uplata na tekući račun prodavnice" tender type — so the warning is not a dead end; optional buyer tag + rolling 365-day total, with the aggregation limit stated in writing to the owner. (b) Deposit-aging on cash_movements: bucket **all** cash received "po bilo kom osnovu", clock from **receipt**, deadline = +7 radni dani shown as a **date**, oldest-bucket-first drawdown for partial pologa, and a NOT-SUBJECT movement type for own-account withdrawals — **but only those the operator asserts were paid out per Pravilnik 77/2011 čl. 2 st. 2 or st. 3**; an unasserted withdrawal stays in the base, because a blanket exclusion errs in the unsafe direction. Advisory only — **no hard block**. (c) **NOT a GTIN column** — `barcode` already exists; add `manufacturer_name` / `importer_name` / `country_of_origin` (čl. 34 st. 1), a separate unused `official_goods_code` for the future šifarnik, `barcode_kind {gtin,internal,none}`, and put the check at **goods receipt** (kalkulacija), not checkout. **Mandatory** rather than advisory **iff the shop sells at distance** (čl. 34 st. 5) | AML čl. 46, čl. 120 st. 2; Zakon 68/2015 čl. 3 st. 1 + Pravilnik 77/2011; ZoT čl. 34 st. 1–5, 8 | **M** (was S — verification enlarged it) |
| SW-12 | **✅ SHIPPED 02.08.2026 — the file, the archive, the till guard and the folder the file lands in. The hosted endpoint is deliberately NOT in it: that is a founder decision (F-13), not an engineering one.** Migration **v19** carries `cenovnik_snapshots` — immutable, with **no `updated_at`**, a `BEFORE UPDATE` trigger covering every identity and content column (**`id` included** — it is the tie-break deciding which row is current and the only handle a divergence record can name, so a mutable key would let that evidence link be repointed) and a second trigger letting `published_at` / `published_target` be written exactly once — plus `products.jedinicna_cena_jedinica` and `jedinicna_cena_sadrzaj_milli` on the schema-wide milli scale (a 0,75 l bottle is `750`). **An outlet's current cenovnik is derived**, `ORDER BY generated_at DESC, id DESC LIMIT 1`, never a stored pointer, so two writers in the same second cannot leave one stale. **`cenovnik.rs` is pure** — rows in, file out. `render_csv` emits the de facto data.gov.rs shape (**UTF-8 with BOM, `;` separator, DD-MM-YYYY, two decimals rendered from integer para at the boundary, 13-digit barcode quoted as text**) in `sifra` order, so the same catalog always renders the same bytes and therefore the same `content_hash` (SHA-256 of the body); `published_prices` is its exact inverse and finds its columns **by name out of the file's own header**, because the till guard has to read whichever file is actually published, including one rendered before a column order changed. **Req. 10 is why the column set has eight entries and not six:** `jedinicna_cena` and `jedinica_za_jedinicnu_cenu` sit beside `prodajna_cena`, because čl. 6 st. 2's second sentence pulls st. 1 into the published file — and an article whose pair the shop has not entered publishes an **empty cell rather than a guessed one**, since čl. 6 st. 4 makes the shop answerable for what it publishes and a missing cell is a visible gap where an inferred one is not. **What may be stored at all is decided once, by `cenovnik::validate_jedinicna_cena`, and every writer of `products` calls it** — the catalog form and the CSV import both, the form rendering the returned defect as a field error and the import as a **row** error the operator fixes in the per-row report. A jedinica naming a measure the goods are not sold in, with no package content to bridge them, publishes the sale price AS the jedinična cena (372,00 po litru for goods sold po komadu) and is refused on both paths; a second copy of that rule is what let an import write the state the form had just started refusing, so there is exactly one. The import also treats an **unmapped or blank** jedinica mere column as „not supplied“ rather than as `"kom"` — an UPDATE leaves `unit_of_measure` standing (`COALESCE`), and only a CREATE, which has no prior truth to destroy, still defaults. **Republish rides the write (req. 11), because čl. 6 st. 3 says *„ažurira u realnom vremenu“* and a nightly batch is a defect against it, not a simplification:** `commands::catalog`, `campaigns` (activation, markdown step, ending), `importer` (**once per batch** — the file is the whole catalog) and `kep_storno::post_nivelacija` (whose returned verdict `commands::kep` republishes on, after its own commit) each thread `record_offered_price_change`'s own answer rather than re-deciding what counts as a price move, and the catalog path adds the one price that log does not watch — the jedinična cena, which moves with the v19 package-content pair and not with `sale_price_minor`. It runs **after `tx.commit()`**, so a publish failure is logged and the price stands: čl. 6 is not a reason a till cannot change a price, and the next price write repairs the file. The write is a plain `INSERT` — **never `INSERT OR REPLACE`, never an upsert**, with its own assertion, because a REPLACE is a DELETE plus an INSERT that no immutability trigger sees. The archive key is minted once and frozen (`settings.cenovnik_prodajno_mesto`), derived from settings and **never accepted from the frontend**, so a typo cannot start a second lineage the trigger then makes permanent. **The archive (req. 14)** lists newest-first per outlet and hands the body back byte for byte, which is what čl. 6 st. 5 asks the trader to enable; `RecordClass::CenovnikArchive` carries the **two-year floor of the čl. 213 zastarelost** on the shared retention table, upward-only. Snapshots in the **cenovnik archive** past that floor **are deleted** by `commands::cenovnik::purge_expired_snapshots` — launch plus the 6-hourly timer in `lib.rs` — which is the **only** code in this crate permitted to delete a published cenovnik, has its own assertion saying so, and never reaches an outlet's current file however old it is. **The till guard (req. 12)** is `sales_assess_price_integrity`, run against the draft cart so the operator learns before the money moves: it **warns and never blocks**, fires only in the above-published direction (a discount is not a breach, and that is also what keeps it to one comparison), names the snapshot it was measured against — čl. 6 st. 4 binds the shop to the file in force, not to the catalog — and `sales_complete` writes a `cenovnik_price_divergence` row into the never-deleted `compliance_log`. **No file a target accepted means no guard and no block** — the comparison reads the newest snapshot whose `published_at` is set, so an archive of files that went nowhere (the `NotConfigured` default) arms nothing, and čl. 6 st. 4 is never cited against a trader who has published nothing; a check that could not run says so rather than reading as an all-clear. **Publication is a trait (reqs. 13, 15):** `PublishTarget`, with `LocalFolderTarget` and a `NotConfigured` default, and req. 13's seven fetchability rules — no login, no CAPTCHA or bot-fight rule, no JS-rendering requirement, no `robots.txt` disallow, no aggressive rate limit, a stable URL per prodajni objekat, the correct `Content-Type` — written into the trait's doc comment as a **contract binding any future implementation**, because čl. 6 st. 5 is a duty to *enable* and putting the cenovnik behind bot protection would manufacture a breach for our own customer. **Two limbs are NOT built and are disclosed as such:** req. 15's hosted endpoint (F-13), and **čl. 6 st. 6's Nacionalni portal otvorenih podataka account**, whose format is expressly keyed to *„standardom koji je propisan podzakonskim aktom“* — and **no such bylaw exists** (§6 items 4 and 12). A shop that already has a website discharges čl. 6 st. 2 today by pointing the local folder at what that site serves; the panel says plainly that the folder is where the file lands and that whether the site serves it is outside this program. Penalty copy is `legal.rs::cenovnik_not_published`, the **tenth** notice — in `all_notices`, in the duplicated inline list, asserted count bumped to **10** — printing the preduzetnik **fixed 100.000 (čl. 210 st. 3)**, halved to 50.000 on payment within eight days of a prekršajni nalog (ZoP čl. 173 st. 1), with the čl. 207 t. 1 → čl. 206 st. 1/4/5 → čl. 209 st. 1 t. 40 escalation ladder and the čl. 213 limitation in its citation; **the pravno-lice 200.000 is unreachable under the preduzetnik profile**, pinned by the guard req. 18 asks for. Frontend: Podešavanja → **Cenovnik** (the duty, the state of the publishing target, the outlet's archive newest-first, any prior file openable byte for byte), the till warning, and the product sheet's unit-price pair — sent on **every** save, because `SaveProductRequest` replaces the whole row and a form that read the pair without sending it back would clear it on the next ordinary edit. **No „objavi sada“ button anywhere**: publication rides the write that moved a price, and a second answer to *„when did this shop last publish“* is the one thing an operator could believe wrongly. **Scope stays exactly where §2b left it.** SETTLED NEGATIVE: there is **no** „trader who has a website“ carve-out in čl. 6 st. 2 and the pilot may not rely on one. **UNRESOLVED POSITIVE:** whether a website-less trader must **create** a site — st. 2 says *„na **svojoj** internet stranici“*, no ZZP provision obliges any trader to have one, and extending a prekršaj to an unwritten duty runs into lex certa (ZoP čl. 3). **Nothing in the app or in this row says the shop is in breach today**, and **no copy dates the exposure to 1 May 2026** — čl. 210 was not in the čl. 220 carve-out | ZZP26 čl. 6 st. 1–5, čl. 210 st. 3, čl. 213; **[REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2b, §3 V2, §4 reqs. 10–18** | S |
| SW-13 | **✅ SHIPPED 02.08.2026** — the split is **physical, not a convention**: class A is `personnel_records` (v18, the 25 tačke of ZEOR čl. 5, `trajno` under čl. 7 st. 2, `REFERENCES users(id)` deliberately **without `ON DELETE CASCADE`** per req. 24, plus a `BEFORE DELETE` trigger); class B is the surrogate `users.id` already carried by `sales`, `kep_entries` and the rest, kept with the accounting document on **ZoRač čl. 8 st. 4 + čl. 28**; class C is `users.pin_hash` / `password_hash` and `audit_events`. `commands/personnel.rs::purge_expired_classes` is the **time-driven** sweep req. 23 asks for — wired into launch and a 6-hourly timer in `lib.rs`, never a command an operator presses — and it takes a `PurgeableClass` enum with exactly two variants, **neither of which is class A**, so reaching the ZEOR register is a type error rather than something a reviewer has to catch; the whole sweep runs inside one transaction fenced by `never_purge_row_counts` / `assert_never_purge_intact`. The credential hash is discarded **at deactivation** (req. 21), not after a window, with the sweep as the backstop for accounts terminated before that rule existed; the access log expires on its own 2-year class. Its audit line records **which class** was discarded and when, and never a discarded value. Username reuse is refused: a username may change only while the account is active **and stays active**, so a rename cannot ride along on the save that deactivates. **CORRECTED 01.08.2026, see [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §3 V3.** Employee lifecycle: deactivate-never-reuse accounts; keep name; purge PIN hash and login history after a configurable period; no hard-delete of identity from the trading ledger. **The claim that "labour-records law requires" the ledger/shift attribution is FALSE** — ZEOR čl. 5 enumerates 25 fields (prezime i ime, matični broj, pol, datum i mesto rođenja, prebivalište, mesto rada, zanimanje, stručna sprema, radno mesto, radno vreme, vrsta radnog odnosa, datum zasnivanja i prestanka, razlog prestanka …) and **not one of them is a sale, void, till session or KEP attribution**. Keeping the *name* is right (ZEOR čl. 5 t. 1 + čl. 7 st. 2). **Ledger persistence rests on ZoRač čl. 8 st. 4 + čl. 28, never on ZEOR** — cite it correctly or the purge design will be built on a provision that does not say what we thought | ZZPL čl. 42, čl. 5 st. 1; **ZoRač čl. 8 st. 4, čl. 28** (ledger persistence); ZEOR čl. 5 t. 1, čl. 7 st. 2 (name only) | M |
| SW-14 | **✅ SHIPPED 01.08.2026** — migration **v17** (`work_time_entries`, `work_time_periods`, `retention_policies`, employee-profile columns on `users`). Decision logic is pure and clock-injected in **`worktime.rs`**: `assess_caps` (čl. 53 st. 2 ≤ 8 h prekovremenog u kalendarskoj nedelji and st. 3 ≤ 12 h dnevno ukupno — **and only those two**; the čl. 57 st. 5 60 h/week preraspodela ceiling is its own rule set and is applied **by the caller**, `commands/worktime.rs::assess_caps_for_employee`, because `assess_caps` cannot see preraspodela and always leaves that leg `false`) and `check_protection` (čl. 87–91 — maloletnik **8 časova dnevno i 35 časova nedeljno per čl. 87**; **the weekly leg shipped 07.08.2026** (`worktime::MINOR_WEEKLY_CAP_MINUTES`, `ProtectionKind::MaloletanNedeljniLimit`) and **refuses the row** rather than asking for a ground, because čl. 87 states the prohibition itself — it is deliberately kept out of `assess_caps`, whose every leg is an overridable čl. 53 cap. **Refused is the write that *raises* the week**, and only that one: a week already above the cap when the guard turns on (`datum_rodjenja` is nullable and unbackfilled, so filling a profile in switches the guards on over rows already recorded) stays correctable **downwards**, and a day of pure absence still records — an append-only register that cannot describe days that happened hides the čl. 274 exposure instead of surfacing it. The total counts only the days the employee **was** under 18, and drops rows whose `dan` is not a civil date. The čl. 88 st. 2 **night** leg is still unbuilt, night hours being a čl. 62 computation over clock times that `DayHours` does not carry — plus a flat ban on prekovremeni i preraspodela per čl. 88 st. 1, čl. 91 st. 1 dete do 3, **čl. 91 st. 2 samohrani roditelj — threshold SEVEN — and `dete_tezak_invalid` with no age limit**, čl. 90 trudnoća/dojenje as a warning, never a block). `commands/worktime.rs` ships seven commands (`worktime_list_month` / `save_entry` / `correct_entry` / `close_period` / `export_csv` / `my_hours` / `notices`), **append-only** — a correction is a new `verzija` row carrying who/when/why, there is no `UPDATE` against `work_time_entries` — with `require_admin` inside the domain function, not the wrapper. A cap breach **records the day and asks for a čl. 53 st. 1 ground**; a čl. 87–91 block refuses the row. **`retention.rs`** is the shared retention table SW11-SW15 §3 req. 42 mandates: `WorktimeClassification` = `trajno` + `never_purge`, structurally unreachable by the go-live reset (`assert_never_purge_intact` runs inside that transaction in `backup.rs`) — **restore is not fenced**: `restore_backup` swaps the database file, so the register returns as the snapshot holds it, the `pre_restore` safety copy is the only protection there and the `backup_restored` event records the never-purge counts on both sides (a count-based refusal cannot separate a legitimately older backup from data loss, and would leave the shop unable to restore); a backup-prune path **does not exist** in the crate; `WorktimeOvertimeLog` = upward-only **3-year** floor applied per record day; `WorktimeDraft` bounded by the period close. Penalty copy is tier-resolved in `legal.rs::overtime_record_missing` (čl. 276 st. 1 u vezi sa tač. 1a) and `legal.rs::overtime_caps_exceeded` (čl. 274 st. 1 tač. 3) and **`legal.rs::preraspodela_caps_exceeded` (čl. 57 st. 5 → čl. 274 st. 1 tač. 4)** — **no ZEOR figure is rendered anywhere**, and SW-14 introduces no fine figure outside `legal.rs` (**known exception, pre-existing (SW-7):** `src/app/reklamacije/ReklamacijeModule.tsx:128` still hard-codes the regime-versioned reklamacija amounts — see the SW-15 row). Frontend: `src/app/worktime/WorkTimeModule.tsx` (monthly grid, cap warnings, period close, absence category behind the role gate) and `MyHoursPanel.tsx` („Moji sati“ — own rows only off a server-side session, **no export control**, ZoR čl. 83 st. 1 + ZZPL čl. 26). Employee-profile fields land in `commands/users.rs` (čl. 87–91 flags + ZEOR čl. 44 st. 2 `zanimanje_sifra` / `kvalifikacija_sifra` as codes) with **no consent UI** — `saglasnost_prekovremeni_od` records that a written consent exists and when. Every rendering carries *„Evidencija prekovremenog rada — ZoR čl. 55 st. 6. Zakon ne propisuje obrazac.“* **Scope RESHAPED 01.08.2026; full encodable spec (31 requirements) in [SW14-VERIFIED-RULES.md](SW14-VERIFIED-RULES.md) §4.** One row per **(employee, calendar date)** — a register shift is **not** an employee's working time, only corroborating evidence. Store **hours** as the primitive in the **15 ZEOR čl. 24 tač. 1 buckets** (incl. the statutory poslodavac/RFZO sick-leave split); absence category is a **closed enum with zero free text**. Validate the **čl. 53 caps** (≤8 h/week overtime, ≤12 h/day total) — the bigger fine. **Preraspodela is a hard branch, not a toggle** (čl. 58 hours are not overtime, so a naive `>8 ⇒ prekovremeni` rule corrupts the penalised register). Age/parental guards (under-18 reject; **samohrani roditelj threshold is 7, not 14**). **No annual overtime counter** — no annual cap exists in ZoR. **No obrazac** — free-form export, but never labelled *"propisani obrazac"*. Two retention classes: derived period-closed classification `trajno`; raw punch events bounded by the period close and marked purge-eligible there — `retention::draft_purge_eligible` is the **gate**, and **no job performs that sweep yet**: `commands::personnel::PurgeableClass` names only `Credentials` and `AccessLog`, so the raw events stay until a sweep is written. **Stays OUT of payroll (F-12): ZEOR čl. 24 tačka 1 is ours, tačke 2–3 are the accountant's** | ZoR čl. 53, 55 st. 6, 57–58, 87–91, 274, 276; ZEOR čl. 3, 23–25, 44; ZZPL čl. 12, 17 st. 2 tač. 2 | M |
| SW-15 | **✅ SHIPPED 31.07.2026** — `settings.rs::ShopProfile` (`pravna_forma` / `pdv_obveznik` / `distance_selling` all tri-state, defaulting to UNSET and never inferred) + `EsirElement` rows as an *interna beleška o proveri*; every SW-11/SW-15 fine figure is resolved from `pravna_forma` inside `legal.rs`, which is the only module allowed to hold one, and a test pins that "privredni prestup" is unreachable under the preduzetnik regime. The SW-7 reklamacija amounts followed on 31.07.2026 (`legal.rs::reklamacija_breach`, regime-versioned as well as tier-resolved — see row 22a). Retention is the one half still open — see row 21. **Onboarding profile — scope CORRECTED 31.07.2026; full spec in [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §3.** `pravna_forma` enum {preduzetnik, pravno_lice}, **default UNSET, never inferred** — every legal string carrying a number resolves its tier from this one field, which is the root cause of all four penalty-tier errors corrected in this revision (plus a CI guard: the literal "privredni prestup" must be unreachable under the preduzetnik regime). **PDV status does NOT gate the 10y floor** — it changes only which statute supplies it and the SW-3 warning text; retention stays unconditional and `retain_until` is upward-only. **Do not offer a paušal/prosto-knjigovodstvo retention profile** to a registered retail shop (derive dvojno knjigovodstvo). New required fact: **does the shop sell at distance** (webshop/Instagram/Viber) — flips ZoT čl. 34 st. 5 and reverses the SW-11c design. ESIR/PFR panel: label it with the real hook **ZF čl. 6 st. 8** (*provera pre otpočinjanja korišćenja* — an unpenalised duty; the sanction bites via čl. 15 st. 1 tač. 3) and state plainly that **no law requires storing or producing these identifiers** — call the stored row *"interna beleška o proveri"*, never *"evidencija"*. Store naziv + **verzija** + IB + tip {ESIR, LPFR} per element per premises; re-confirm annually or on any reported version change. Per-premises L-PFR assertion (čl. 6 st. 4 + čl. 15 st. 1 tač. 4). **Hard rule: these identifiers must never appear on any output that itemizes a sale** | ZF čl. 6 st. 4, st. 8, čl. 15 st. 3; ZoT čl. 34 st. 5; ZoRač čl. 28; ZPPPA čl. 114ž; ZAG | S |

**P2 — later:**

| ID | Build | Legal driver | Effort |
|---|---|---|---|
| SW-16 | **✅ SHIPPED 03.08.2026 — a two-phase blind count in which the blindness is a property of the query layer, not of a hidden column.** Migration **v20** carries `popis_sessions`, `popis_lines`, `popis_signatures` and `popis_commission`, and enforces two module invariants in the engine itself, because migrations are append-only and neither could be added later without a second migration: **the čl. 8 st. 5 blind count is a write guard** — a `knjigovodstvena_kolicina_milli` is refused while the session is in `draft`/`counting` **and while no `faza = 'a'` potpis exists for it**, anchored on the potpis rather than on `status` because `status` is a claim any `UPDATE` can make and a session can be born in `counted_signed` — and **posting write-locks the session, its stavke, its komisija and any new potpis** (req. 41), with `popis_signatures` immutable from the moment each signature is taken. No popis table is ever written with `INSERT OR REPLACE`: a REPLACE is a DELETE plus an INSERT that no `BEFORE UPDATE` trigger sees. **`popis.rs` is the state machine** — `draft → counting → counted_signed → computed → computed_signed → posted`, with all thirty (status, event) pairs swept exhaustively and the error *code* asserted rather than a bare `is_err` — and the čl. 8 st. 5 predicate is deliberately split in two: `book_quantities_visible(status)` is only the *status* limb and is **module-private**, so „consult the whole condition“ is a compile error rather than a doc comment, while `book_quantities_released(status, phase_a_signed)` is what a query layer must call. **The v20 guard protects one column; the duty is wider** — the perpetual stanje also sits in `inventory_balances` and is derivable from `inventory_movements`, both readable with no trigger in the way — so `read_lines` is **two statements rather than one and a filter**, naming neither those tables nor that column while the data is withheld, and the same rule reaches the earliest screen there is, the čl. 8 st. 4 nivelacija scope handover, which hands over four fields and **no količina from any source**. Every blind assertion is made against a **seeded perpetual balance that must appear nowhere in the serialized response**, so the withheld half cannot pass vacuously. `popis_sign_phase_a` writes in the statutory order — potpis, then state, then the book quantities read out of `inventory_balances` at the earliest moment čl. 8 st. 5 permits reading them at all — and the order is behaviourally proven rather than commented: reversing it fails five tests, because the v20 guard aborts a book quantity written before its potpis. A šifra that resolves to nothing keeps its NULL rather than a zero, since the gotovina and potraživanja liste have no perpetual record behind them and a fabricated zero would report a manjak the shop does not have. **Corrected 03.08.2026:** the blind read carved `cena_minor` out for the gotovina lista only, which left the **čl. 12 st. 2 iznos write-only during the count** — the till took it, the row came back „—“, the edit form reopened empty, and the Phase A `snapshot_hash`, taken from that same read, recorded it as absent under a potpis the commission had given over it. The carve-out now reaches both liste whose „cena“ is the commission's own finding rather than an obračun figure, and the freeze follows the read: a moved iznos is refused in the obračun exactly as a moved apoen is. The other four liste are unchanged and are asserted as the negative control. The signature step also cited **čl. 9 st. 3 for both potpisi**; it now carries the article of the potpis actually being taken, so the čl. 8 st. 5 step no longer quotes the *„uz štampanje“* rule that governs the liste signed after it. **The rok is computed, never tabulated** (req. 38): `izvestaj_due` gives `rok za predaju FI − 60 dana` for the godišnji popis and `datum popisa + 30 dana` for the nivelacioni, and the FY2027 leap answer (31.01.2028) is exactly the case a hardcoded table gets wrong; the filing deadline is **configuration** (`settings.popis` → `rokPredajeFi`), an unset one **refuses the annual izveštaj by name rather than presuming 31 March**, and the module is scanned both for a `gggg-MM-dd` literal and for every wall-clock reader `src/clock.rs` actually exports. Čl. 14 st. 2 gives the odluka o usvajanju the same rok, so it gets one date and not a second engine. **All six liste are first-class** (req. 36) — roba · oštećena/zastarela (čl. 10 st. 3) · van objekta (čl. 10 st. 4) · gotovina po apoenima (čl. 11 st. 1) · nedokumentovana potraživanja/obaveze (čl. 12 st. 2) · konsignaciona roba (čl. 2 st. 5) — and all six are rendered **including the empty ones**, since which liste a popis has *not* touched is what čl. 10–12 actually ask. **Presence is declared, not stored:** nothing in this database records that part of the stock is damaged or that a rail belongs to somebody else, so the person taking the popis declares it **on the count sheet, where it can still be acted on**, and `ensure_liste_kompletne` makes the declaration binding on the izveštaj. The apoen is `cena_minor` counted in whole notes and coins, **with no denomination whitelist** — refusing an apoen outside the RSD notes in circulation would present a prudential narrowing as čl. 11 st. 1 and would refuse a lawful count of devize — and the čl. 2 st. 6 reminder is derived from the liste themselves, carries `datum popisa + 10 dana` and says plainly that **the application does not deliver the lista to the owner**. **The izveštaj carries the eight čl. 13 st. 1 elements as a closed enum** (req. 37), five written by the komisija and three computed so the shop is never asked to retype the stanje it has just counted; it is **composed on demand and stored nowhere**, and it says so — a shop that typed five paragraphs and closed the screen would otherwise lose them without being told. It is refused during the count on the same čl. 8 st. 5 predicate, because a document carrying the knjigovodstveno stanje hands the komisija through prose exactly what a hidden column keeps back. **No natural totals anywhere:** stavke on one lista can be in komadima, metrima and kilogramima at once, so the naturalna razlika stays per stavka where čl. 9 st. 1 t. 4 puts it and the izveštaj totals money plus counts of viškova i manjkova. **Nivelacija is a first-class mode** (req. 33): the obligation is derived from `price_history` and **not from `kep_entries`** — SW-9b writes no ledger row when on-hand is 0, while ZoRač čl. 21 attaches to the price change and says nothing about stock — it is **reported and never auto-created**, since a session the app opened would assert a čl. 20 st. 3 usklađivanje nobody performed, and **only a `posted` nivelacija popis discharges it**. The narrowing to repriced articles is offered as a **`[PRUDENTIAL]` preporuka**, with the Pravilnik 140/2004 čl. 16 analogy **and** the fact that that regime is unavailable here stated in the same breath, guarded mechanically: no obuhvat string may contain a duty word (`morate`, `dužni ste`, `dužan je`, `obavezno`, `obavezan je`, `nalaže`). `kep_nivelacija` returns the notice and `KepModule` renders it at the till — the moment the shop changes a price is the only moment it is certain to be looking — and walking a nivelacija popis to `posted` leaves the KEP fingerprint byte-identical, which is what „two obligations on one event“ has to mean. Penalty copy is `legal.rs::popis_not_conducted`, the **eleventh** notice — in `all_notices`, in the duplicated inline list, asserted count bumped to **11** — printing the preduzetnik **čl. 58 prekršaj, 100.000 do 500.000**; the provision that actually describes the failure, **čl. 57 st. 1 tač. 12), is a *privredni prestup* that ZPP čl. 6 st. 1 confines to a pravno lice i odgovorno lice**, so reading its 100.000–3.000.000 band onto the pilot would overstate his ceiling roughly sixfold, and **„3.000.000“ is now a blanket needle in `FORBIDDEN_TO_A_PREDUZETNIK`**. The summary carries both duty limbs (čl. 20 st. 2's balance-date popis and čl. 21's price-change one), the legislated čl. 20 st. 3 ordering, and the Pravilnik's penalty gap **in both directions** — 89/2020 has no kaznene odredbe of its own, *and* a procedural defect is still reachable on the argument that the popis was not taken „u skladu sa ovim zakonom“. Retention is `RecordClass::PopisDokumentacija` on the shared table with the **5-year floor of ZoRač čl. 28 st. 7**, counted by a new clock — `business_year_floor`, which resolves every day of one business year to the same 31 December per **st. 9**, so a nivelacija counted in May and the godišnji popis of 31 December expire together instead of the first going seven months early. The class **holds personal data** — `popis_commission` names each popisivač and `popis_signatures` records who signed — which is why it is the twelfth čl. 47 radnja (`popis_imovine`). **No automatic deletion exists for it:** `popis_purge_eligible` is a gate with no production caller, and the stored napomena says so rather than promising an archive this application does not keep. Frontend: **Popis** in the shell (`CountSheet`, `IzvestajPanel`, `PopisModule`), keyed on the backend's own `knjigovodstvoDostupno` and **never on `status`**, which would put back in the UI the shortcut the compiler was made to refuse — no book-quantity input at all while the count is blind, an edit affordance for exactly the writes the backend accepts, three different reasons for the three closed states, and the req. 40 komisija warning that names the person, cites čl. 5 st. 1, says the čl. 6 st. 1–2 shodna primena **is not settled** and says the popis was not stopped. **Reqs. 31/32/35 shipped 07.08.2026, and what follows is their current state rather than the disclosure they answer** — that disclosure, namely that čl. 9 st. 3's *„uz štampanje“* had no code behind it and that req. 35 recorded no approval at all, stays dated in `docs/PROGRESS.md`, which is the record of what was true when. `popis_export_lista`, `popis_export_odluka` and `popis_export_plan_rada` write three documents into `exports/`, `popis_print.rs` renders them, and `PopisModule` hands each written file to `PrintService.openForPrint`; the members sign the paper, and the exported file is not a signed isprava. **The čl. 8 st. 5 sheet withholds structurally:** the phase is derived from `book_quantities_released(status, faza_a_potpisana)`, the frontend may narrow it to Faza A and may never ask for Faza B, and the Faza A renderer is a code path that never names `knjigovodstvena_kolicina_milli` or `razlika_milli` at all — blanking them would have looked identical from outside and been the wrong thing. An explicit Faza A is honoured in every state, because čl. 2 st. 6 gives the owner of tuđa roba ten days to receive a primerak of the **signed** posebna popisna lista and that reprint has to survive the obračun. **And one lista prints on its own**, which is the document čl. 2 st. 6 actually names: `popis_export_lista` takes a `lista` narrowing, `potpisni_blok` is emitted once per lista rather than once per document — čl. 8 st. 5 has the members sign *„те листе“*, each of them — the sections break onto pages of their own, and `PopisModule` offers a print action per lista that has stavke. The delivery stays the obveznik's; the app writes the paper and sends nothing to anybody. Req. 32's header block (obveznik, PIB/MB, maloprodajni objekat, broj liste, datum i period) and the three vrednost columns are on the exported lista; **the editable default template is not, and that limb of req. 32 stays open** — `zaglavlje_kolona` and the two row renderers are a fixed layout with no setting and no operator control over it, and nothing operator-facing says otherwise. The **izveštaj o popisu is neither printed nor exported**: `popis_izvestaj` composes it on demand and returns a view for the screen, and the printed, signed copy is the obveznik's own to make and keep. The stored retention napomena, this register's own `popis_imovine` entry and the izveštaj's closing upozorenje each keep that denial verbatim and each now carry a second sentence naming the three documents the program does write — a bare denial read beside a module that exports three files is the same defect facing the other way. **Req. 35 — the odluka o popisu i obrazovanju komisije and the plan rada:** v21 added `plan_rada_odobrio`, `plan_rada_odobreno_at` and `odluka_doneta_at`; `popis_odobri_plan` records the PoP čl. 8 st. 2 approval against a name the operator supplies — a blank one is refused rather than filled in from the registered obveznik, because a server-side default is an auto-approval wearing a default's clothes — `DokumentiPanel` is the screen, and `popis_print.rs` generates both documents off the same header block the popisne liste carry. A roster that is empty or mixed is declared **neither** a komisija nor a jedno lice iz čl. 6 st. 1: the odluka *is* the appointment, so printing it before anybody is appointed is its primary use. **The application holds the approval and not the schedule:** nothing writes `plan_rada_json` after `popis_open`, so wherever it is empty the plan rada and the screen both say the *odobrenje* is recorded and the *sadržina* is not, in the same čl. 8 st. 1 terms. **`odluka_doneta_at` is written by no verb in this build** — exporting a draft odluka is not *donošenje odluke* — so the column stays empty, and the odluka's own header cell says the date is not recorded in the application and is to be entered on the printed primerak. **Req. 40's second limb is likewise uncoded:** the named popisivači, the `rukuje_imovinom` flag and the warn-never-block behaviour are present and correct, but *„reuse the flag for ZoRač čl. 10 st. 5“* (control of računovodstvene isprave) has **zero occurrences in the crate** — the flag is read in exactly one place, the komisija warning. And **`perpetual_odluka_ref` is validated and then never consumed**: the req. 34 gate refuses a reference with no posted in-year popis behind it, after which the column is only read back, so **no code path shortens or skips the count** and čl. 9 st. 2's *„the exception excuses step 2) alone“* is not modelled at all. That is the conservative direction and nothing is unsound, but the field today buys a gate and no behaviour. The čl. 9 st. 2 shortcut checks the *izvršen i proknjižen* limb and **not the *usvojen* one**, because no schema records the čl. 14 st. 2 odluka — the module states that it does not record that decision rather than implying it does. **Three open items travel with the module** (full text in [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §6): **R-5** — whether the goods-handler exclusion applies *shodno* to a one-person popis; handled as **warn, never block**. **R-6** — whether a purely electronic potpis satisfies čl. 9 st. 3; print-and-sign is the default and the signature step says in as many words that recording it here **does not replace the potpis on paper**. **R-7** — no provision names popisne liste expressly for retention; the five years travel as the inference they are, and the napomena says so. **Scope CORRECTED 01.08.2026 — the old spec mandated a NON-COMPLIANT screen. See [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2c.** **PoP čl. 8 st. 5 forbids releasing book quantities to the popis commission before the counted state is written into the liste and the members have signed them** — the old "counted vs book qty" side-by-side count sheet would put the customer in breach. Build a **blind count** with **two signature events** (čl. 8 st. 5, then čl. 9 st. 3), and only reveal book quantities after the first signature. **No obrazac exists for the popisna lista** (Pravilnik 89/2020 runs čl. 1–16 and ends at the minister's signature — no prilog, no column list), so the layout is ours — **but the *izveštaj o popisu* HAS prescribed content (čl. 13 st. 1)** and must be a structured template, not free text. **Scope: binds only double-entry preduzetnici** (ZoRač čl. 4 st. 1 + čl. 2 t. 3) — and a retail boutique necessarily is one: ZPDG čl. 40 st. 2 t. 2 excludes retail from paušal (the st. 3 hatch covers only a kiosk/prikolica/pokretni objekat) and čl. 43 st. 3 confines prosto knjigovodstvo to poljoprivrednik/drugo lice. **Do NOT build the Pravilnik 140/2004 prosto-knjigovodstvo column set** — that regime is legally unavailable here. Additional trigger the old row missed: a popis is mandatory **on change of retail selling prices** in a maloprodajni objekat (ZoRač čl. 21; PoP čl. 3) — which ties directly to the KEP nivelacija already shipped | ZoRač čl. 4, 20, 21, 28 st. 7 i st. 9, čl. 57–58; PoP čl. 2, 3, 8 st. 5, 9 st. 3, 13; **[REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2c, §3 V5, §4 reqs. 29–42** | L |
| SW-17 | **✅ SHIPPED 02.08.2026** — `data_breaches` (v18) + `commands/breaches.rs` + Privatnost → Povrede podataka. **It provides the record required of the rukovalac by ZZPL čl. 52 st. 6–7 — it does not by itself „satisfy“ the article**, whose st. 1–5 duties run to the Poverenik and to the affected individuals and are discharged by people, not by a screen. **Log first, decide notifiability second** (req. 43): st. 6 binds the rukovalac to document *„svaku povredu“* with no risk qualifier, so notifiability is a **derived flag on a row that always persists**, never a wizard gate that discards the No answers. Beyond the three st. 6 elements the row carries what st. 7 needs (req. 44): the **immutable `saznanje_at`** that starts the clock — a `BEFORE UPDATE` trigger refuses to move it, because a movable anchor makes the st. 2 delay reason optional in hindsight — separate occurred/discovered instants, the risk outcome, the explicit notify/do-not-notify decision **with reasoning**, the Poverenik-notified stamp, a **st. 2 delay reason that becomes mandatory once 72 h have elapsed since saznanje**, and a čl. 53 block recording either that the individuals were told or which st. 3 exception was relied on. `breaches_export_obrazac` renders the **Pravilnik 40/2019 obrazac** in its five prescribed sections down to the mesto/datum + Ime i prezime + Potpis block and the „Prilog:“ slot, for print and scan — **no submission API exists**, and the countdown is the flat **72 h of Pravilnik čl. 3**. Never a roster of names: the obrazac asks for *broj lica*, and the row holds a count. Admin-only, and excluded from routine exports and support bundles (req. 48). **The fine printed here is the one for not notifying — preduzetnik 20.000–500.000 (čl. 95 st. 1 tač. 24 u vezi sa st. 4), resolved in `legal.rs::breach_notification_missing`. No fine is printed for a documentation-only failure**, because none is unambiguously prescribed for st. 6 alone — but without that documentation there is nothing to prove the article was complied with, which is how a missing log converts back into the notification charge | ZZPL čl. 52 st. 6–7; Pravilnik 40/2019 | S |
| SW-18 | e-Otpremnica (SEO) integration slot in the future goods-receiving flow: confirm physical receipt ≤3 working days; e-prijemnica accept/reject ≤8 days (needed 1 Oct 2027 for VAT-registered shops) | ZEO čl. 7–8, 16 | L |
| SW-19 | Optional per-sale buyer identifier (type-prefixed: 10=PIB, 11=JMBG; composite public-funds format TBD §6) + B2B turnover report | PVFR + PU šifarnik; future-proofing | S |
| SW-20 | **DESCOPED 01.08.2026 — not a compliance item. See [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2d.** Three independent reasons: (i) **care and size labelling ceased to have effect on 1 January 2023** — Pravilnik čl. 28, as substituted by **86/2019** (the gazette number the old row omitted): *„Одредбе чл. 21–24. овог правилника престају да важе 1. јануара 2023. године.“*, killing čl. 21 (care, SRPS EN ISO 3758), čl. 22 (shrinkage), čl. 23 (size) and čl. 24; (ii) only *sirovinski sastav* survives (čl. 16 st. 1) and its creation duty is the **proizvođač's (čl. 15 st. 1) or uvoznik's (čl. 15 st. 2)** — the retailer is a *distributer* (ZTZP čl. 3 t. 7) with a verify-only duty; (iii) every operative rule regulates a **physical artefact** — the tag, catalogue, commercial documentation or packaging — and fiscalization requires *naziv artikla*, not composition. **Re-file the attributes in the product backlog.** Three carve-outs: **size/colour variant handling stays** as merchandising functionality (a boutique genuinely needs a variant matrix and per-size stock — do not let a compliance descope kill a real product requirement); **čl. 16 st. 3 arms if e-commerce is ever built** (composition must be visible pre-purchase incl. electronic media, in Serbian per st. 7, with ZoT čl. 34 st. 5 independently); and **if the pilot ever imports directly on its own name it becomes an uvoznik** under čl. 15 st. 2 and inherits the full label duty — ask the shop (§6 item 15). Caution: čl. 1 (Predmet) still *recites* care/shrinkage/size — residual drafting debris that cannot revive articles čl. 28 repealed, but it will make a spot-checker think this row is wrong. Serbian secondary literature on this topic is systematically stale (it predates the sunset) | Pravilnik o označavanju i obeležavanju tekstilnih proizvoda, Sl. glasnik RS **1/2014 i 86/2019**, čl. 15–16, čl. 28; ZTZP čl. 3 t. 7 | **descoped** |

---

## 4. Founder action list (non-software)

**F-1. Sign the čl. 45 DPA (ugovor o obradi) with the pilot shop — before the first support session.** Start from the Poverenik's standard contractual clauses (Odluka, Sl. glasnik RS 5/2020, issued under [ZZPL čl. 45 st. 11](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html)). Statutory content checklist:
- **Identifiers (st. 3):** predmet i trajanje obrade; priroda i svrha (scope: "remote technical support and maintenance of VantumPOS"); vrsta podataka o ličnosti (employee identity, credentials-related metadata, shift/sales attribution); **vrsta lica** (zaposleni prodavnice); prava i obaveze rukovaoca.
- **Eight processor duties (st. 4 tač. 1–8):** (1) process only on documented written instructions, incl. any transfer abroad; (2) staff confidentiality (contractual or statutory); (3) čl. 50 measures; (4) sub-processor conditions per st. 2 i 7; (5) assist with data-subject rights (Glava III); (6) assist with čl. 50 and čl. 52–55; (7) delete or return all data + copies at termination unless law requires retention; (8) make available all compliance information; enable audits.
- **Plus:** st. 5 duty to warn of unlawful instructions; sub-processor annex with prior written (general or specific) authorization + objection mechanism (st. 2) and flow-down/full liability (st. 7); breach clause fixing čl. 52 st. 3 at a concrete ≤24h with st. 4 content; session-logging/audit rights tied to SW-10; note that own-purpose processing makes Actaer a controller (st. 8). Flag the AnyDesk transfer/sub-processor question for the lawyer (§6).

**F-2. Decide the support model in writing:** either strict incidental-access (rare bug-fix sessions, shop employee drives, staff avoid personal-data screens, confidentiality clause) per [EDPB 07/2020 para 83](https://www.edpb.europa.eu/system/files/documents/2023-10/EDPB_guidelines_202007_controllerprocessor_final_en.pdf), or routine support = processor. Default: sign the DPA regardless (F-1).

**F-3. Ship a one-page "Obaveštenje o obradi podataka o ličnosti zaposlenih" template** with onboarding, covering all [ZZPL čl. 23](https://www.paragraf.rs/propisi/zakon_o_zastiti_podataka_o_licnosti.html) items: controller identity/contacts; purposes + legal basis (čl. 12 st. 1 tač. 2–3); recipients (Actaer during support); no transfers abroad (pending F-1 AnyDesk answer); retention criteria; rights (access, rectification, erasure, restriction, objection, portability, Poverenik complaint); whether provision is obligatory + consequences; no automated decision-making. Fine for omission (**tier CORRECTED 31.07.2026**): 50k–2M RSD binds a *pravno lice* (čl. 95 st. 1); a **preduzetnik faces 20.000–500.000 (čl. 95 st. 4)**.

**F-4. Ship pre-filled čl. 47 records:** controller record (7 items) for the shop; Actaer's own slim processor record (4 items) covering support engagements. Do **not** designate anyone "DPO" — use "kontakt za zaštitu podataka" (voluntary DPO triggers publish-and-notify). **Tier CORRECTED 01.08.2026 — the same defect class the 31.07 sweep exists to remove, missed here because it was written "100k" rather than "100.000":** the fixed 100.000 is čl. 95 **st. 2**, expressly a *rukovalac koji ima svojstvo pravnog lica*. A **preduzetnik faces fixed 50.000 (čl. 95 st. 6)**. The tačka itself (st. 2 tač. 6) is **UNVERIFIED** — it appears in neither verified-rules memo; the amount holds regardless because st. 6 covers every st. 2 prekršaj, but do not quote the tačka as settled.

**F-5. Send the written pitanje to PURS** (kontakt: budiefiskalizovan@gov.rs / pisano obraćanje PURS) — exact question in §6 item 1. Required before scaling beyond the pilot; not required for the double-ring pilot itself.

**F-6. Fix the posture sentence in every contract, manual and marketing page:** "VantumPOS nije elektronski fiskalni uređaj niti ESIR i ne izdaje fiskalne račune; promet na malo se evidentira preko odobrenog ESIR-a prodavnice." Never use "kasa/fiskalna kasa/ESIR" for the product.

**F-7. Write the internal compliance-posture memo** (non-fiscal purpose; ledger immutability design; reset is a logged, admin-gated pre-production tool) — the standing rebuttal to any ZPPPA čl. 175b "služi za izbegavanje" characterization, protecting Actaer and the holding shop.

**F-8. Pilot onboarding checklist:** verify shop's ESIR in the [Registar](https://www.purs.gov.rs/sr/eFiskalizacija/registar-odobrenih-elemenata-efu.html) (naziv, verzija, IB, active rešenje); record legal form (archive-law scope) and PDV status; train "ESIR first, VantumPOS second" incl. provokativna kupovina risk (čl. 12 closure); brief on korporacijska kartica / public-sector e-faktura duty (ZEF čl. 3); document in writing any refusal of "skip the ESIR" requests.

**F-9. Actaer's own archive-law compliance** (it is a pravno lice): open an arhivska knjiga, adopt the lista kategorija sa rokovima čuvanja and obtain the competent archive's saglasnost, deliver the prepis by 30 April annually, never destroy documentary material without written archive approval ([ZAG čl. 9, 14, 16, 65](https://www.paragraf.rs/propisi/zakon-o-arhivskoj-gradji-i-arhivskoj-delatnosti.html)).

**F-10. Lawyer confirmation list (cheap, batched):** verbatim ZF čl. 15–18 fine amounts against the gazette PDF; AnyDesk = transfer abroad / sub-processor (§6.5); retention classification of POS shift data (§6.7); ZZP26 čl. 6 scope for a shop without a website (§6.4); čl. 56 stav numbering for the DPO publication duty before quoting it in any document.

**F-11. Breach runbook** (one page, both parties): Actaer→shop ≤24h (contract); shop→Poverenik ≤72h on the [Pravilnik 40/2019 form](https://www.paragraf.rs/propisi/pravilnik-o-obrascu-obavestavanja-poverenika-za-informacije-od-javnog-znacaja-o-povredi.html), scanned copy to povredapodataka@poverenik.rs; internal documentation of every breach; direct notice to employees only on high risk (encrypted-backup exemption applies once SW-2 ships).

**F-12. Keep VantumPOS out of payroll** (wage records are kept trajno and belong with the shop's accountant — scope discipline, not a feature).

**F-13. Decide whether Actaer hosts the cenovnik endpoint (ZZP26 čl. 6 st. 2 + st. 5) — the one SW-12 limb the software deliberately did not build.** SW-12 shipped everything up to the file: it renders the cenovnik, archives every published version, guards the till against ringing above it and writes it into a folder the shop nominates. What it does not do is put that folder on the internet, because čl. 6 st. 2 requires publication *„na svojoj internet stranici“* and a shop without a site has no folder to point at. Three questions, in order: **(a)** does Actaer run a public endpoint carrying its customers' prices — that is a hosting service with its own uptime, its own cost and its own liability if the file is wrong or unreachable, and čl. 6 st. 4 binds the shop to whatever it serves; **(b)** if yes, the seven fetchability rules written into `cenovnik::PublishTarget`'s doc comment are the acceptance criteria (no login, no CAPTCHA or bot-fight rule, no JS-rendering requirement, no `robots.txt` disallow, no aggressive rate limit, a stable URL per prodajni objekat, `text/csv; charset=utf-8`) — a WAF default would manufacture a čl. 6 st. 5 breach for the customer; **(c)** separately, čl. 6 st. 6's account on the Nacionalni portal otvorenih podataka is **not implementable today** — st. 6 keys the format to a standard *„propisan podzakonskim aktom“* and that bylaw does not exist (§6 items 4 and 12), so this waits on the monitoring trigger, not on engineering. **Do not build a sales motion on „you are already illegal“** — whether a website-less trader must create a site is unresolved (§2b), and the pilot must not be told it is in breach.

---

## 5. Certification path map — **NOT NOW; for a later decision**

If Actaer ever wants VantumPOS to issue fiscal receipts, the path is fully mapped and routine (~120 rešenja issued H1 2026; IBs ≈1.580). Decision inputs:

**Process** ([Uredba čl. 8, 11](https://www.paragraf.rs/propisi/uredba-o-obliku-i-sadrzaju-registra-elemenata-vrstama-elektronskih-fiskalnih-uredjaja.html); [TU odeljci 4, 7](https://purs.gov.rs/upload/media/2025/2/4/414144/Tehnickouputstvo-ESIRiliL-PFR.pdf)): (1) free registration at https://tap.sandbox.suf.purs.gov.rs/ (open to d.o.o. **and** preduzetnik; no commitment) → (2) e-mail confirmation → (3) PURS approves registration → (4) PFX certificates by e-mail unlock dev environment, build instructions, L-PFR auto-test tools, test bezbednosni elementi → (5) develop, file prijava za tehnički pregled with the full self-assessment questionnaire (documentation in Serbian) → (6) **tehnički pregled: iterative, NO statutory deadline** ("Potrebne tehničke izmene"/"Dostavljene tehničke izmene" round-trips) → (7) zahtev za odobrenje starts the **15-day** statutory clock → (8) administrativni pregled → (9) rešenje with IB + izveštaj o postupku odobravanja → (10) publication in the public register. Approval sought: **PRENOSIVO** (sold to shops), classification **OSNOVNI** (Promet/Kopija/Predračun/Obuka × Prodaja/Refundacija) or **NAPREDNI** (+ Avans) — TU 7.2.3 fixes the receipt matrix; part 17 requires sample receipts with readable QR (40×40–50×50 mm) incl. sa-ID-kupca variants.

**Deterministic engineering gap list vs today's code** (TU 7.4.2, all Obavezno): receipt printing stack — electronic AND printed, screen-only fails (deo 10 P10); hard block on issuing anything without PFR data (deo 9 P1 — the exact inverse of today's `not_fiscalized` flow); ESIR–PFR mutual authentication at startup (10 P2); tax rates exclusively from PFR/SUF, never local (deo 12 P1, P6); GTIN (10 P11); mandatory reference number for refunds/copies (10 P12); 30-day searchable electronic journal (10 P16); all 7 payment methods (Other/Cash/Card/Check/WireTransfer/Voucher/MobileMoney) plus the cash-consolidation mode switch — note PVFR čl. 6 st. 2's reduced set is **mandatory only for food/beverage-on-site and bakeries** (10 P7; split payment optional P8); item price rounding 2 decimals half-up (11 P4); item selection by name or GTIN scan (11 P5); product import/export (11 P6); Wi-Fi ESIR-to-PFR obavezno (deo 6 P1); "OVO NIJE FISKALNI RAČUN" ≥2× font on Kopija/Predračun/Obuka (deo 16 P10).

**Re-approval tax:** every change to an approved element requires a new zahtev ([Uredba čl. 9](https://www.paragraf.rs/propisi/uredba-o-obliku-i-sadrzaju-registra-elemenata-vrstama-elektronskih-fiskalnih-uredjaja.html)); PURS operationalizes as "svaka nova verzija koja utiče na izgled fiskalnog računa zahteva novo odobrenje" (TU 7.1), each version getting its own IB. **Architect the fiscal module as a small, isolated, version-pinned surface.** PURS performs field checks comparing deployed vs approved builds and can revoke by rešenje (halt sales + market withdrawal) — release management must guarantee shops run exactly the approved fiscal-module version (TU odeljak 6; Uredba čl. 10–11).

**Shop-side economics:** physical retail requires ≥1 L-PFR per poslovni prostor — V-PFR alone is legal only for pure internet / used-own-assets sales ([ZF čl. 6 st. 3–4](https://www.paragraf.rs/propisi/zakon-o-fiskalizaciji-republike-srbije.html); prekršaj čl. 15 st. 1 t. 4); V-PFR is free and PURS-hosted. Approved software L-PFRs cost ~€5/month (Master LPFR verified: 5€/mo or 50€/yr + PDV per location, ≤4 ESIRs; [mastersoftware.rs](https://www.mastersoftware.rs/sr/resenja/master-lpfr/)) plus a smart-card reader; the bezbednosni element is PURS-issued per premises to the **merchant** via ePorezi (Actaer cannot obtain it; renewal 1–4 years) ([Pravilnik o BE, 31/2021 i 96/2021](https://www.paragraf.rs/propisi/pravilnik-o-uslovima-i-proceduri-izdavanja-i-nacinu-koriscenja-bezbednosnog-elementa.html)). **No state fee for approval was found anywhere** (unconfirmed as a negative — §6.8). Realistic timeline for a near-compliant product: ~2–4 months sandbox-to-rešenje (2021 first wave ran 3–5 months); the unbounded variable is the tehnički pregled queue. A "partner with a certified cloud ESIR API" shortcut exists (e.g., efiskalizacija.cloud, IB 1522) but is legally awkward for in-store desktop retail — the checkout UI arguably becomes part of the element — so the clean options remain: stay non-fiscal (current) or certify an OSNOVNI/NAPREDNI prenosivo ESIR module.

---

## 6. What remains genuinely unverified

| # | Open item | What we tried / why unsettled | Exact question to put | Risk of proceeding without it |
|---|---|---|---|---|
| 1 | **PURS blessing of the handoff pattern** | Full PURS FAQ grep (only ERP item = reprint-as-Kopija); budiefiskalizovan.gov.rs ECONNREFUSED; MF mišljenja searches empty (published only in monthly Bilten PDFs, unindexed) — legality is inferential from ZF čl. 2/6 + TU P17/P6 + market practice | "Da li softver koji nije odobreni element EFU sme da prenosi podatke o pojedinačnom prometu odobrenom ESIR-u putem ulaznog kanala dokumentovanog u odobrenju tog ESIR-a (fajl-ulaz po TU 7.4.2 tač. 10 P17 ili API), pri čemu ESIR izdaje fiskalni račun u momentu prometa — i da li takav softver sam po sebi predstavlja ESIR koji zahteva odobrenje po čl. 2 st. 1 tač. 5 Zakona?" | Recharacterization: shop čl. 15 fines + čl. 12 closure; Actaer čl. 18. Low for the double-ring pilot; material before any integration or scaling |
| 2 | Whether feeding an ESIR via an interface NOT declared in its approval voids reliance on the odobrenje | Registry records no interfaces; approval dossiers not public | Ask the target ESIR vendor for its self-assessment answers (P17/API declared?) or written confirmation the interface is covered; optionally fold into item 1 to PURS | Integration built on an undeclared channel may fall outside "as reviewed" — same recharacterization exposure |
| 3 | Timing tolerance of handoff ("u momentu prometa" — is seconds-level async file pickup acceptable? which provision fixes the moment?) | ZF čl. 8 st. 1 real-time verified; moment-of-issuance mechanics live in PVFR — not verbatim-verified for async tolerance | Lawyer: precise article fixing issuance timing and accepted latency in inspection practice | An integration architected around a queue could be per-se non-compliant |
| 4 | **HALF-CLOSED 01.08.2026, see [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2b.** SETTLED: there is **no** "trader who has a website" carve-out in čl. 6 st. 2 — the pilot may not rely on one. STILL OPEN: whether a website-less trader must **create** one (st. 2 says *"na **svojoj** internet stranici"*, and no ZZP provision obliges any trader to have a site; extending a prekršaj to an unwritten duty runs into lex certa, ZoP čl. 3). Also open: the open-data-portal standard. **SW-12 shipped 02.08.2026 against exactly this split** — the capability is built, the copy asserts no breach, and the hosting half is F-13 | Ministerial bylaw (čl. 6 st. 7) not found — **verified negatively across all 36 Sl. glasnik RS issues from 36/2026 (24.04.2026) through 71/2026 (31.07.2026), plus must.gov.rs**; čl. 216 gives the Minister until **01.05.2027**. No mišljenje, no inspectorate guidance, no case law on the no-website case | Ministarstvo trgovine/lawyer: does st. 2 create a duty to create a website; publication standard and format | **Risk figure CORRECTED — the old "200k" was the *pravno lice* tier printed against a preduzetnik pilot.** Preduzetnik: fixed **100.000** (čl. 210 st. 3), halved to 50.000 on payment within 8 days of a prekršajni nalog (ZoP čl. 173 st. 1). **Export format may need rework: the shape SW-12 ships is the live data.gov.rs practice, NOT law** — no format is prescribed while the čl. 6 st. 7 bylaw is missing, so the shipped column set is **practice rather than law**. It is **not configuration**: the mapping is the constant `cenovnik::COLUMNS`, so swapping it is a code change and a release we ship — not something the shop can switch (§4 req. 16's *„column mapping in configuration“* is stronger than the code; see the SW-12 disclosure in [PROGRESS.md](PROGRESS.md)). **Do not build a sales motion on "you are already illegal."** |
| 5 | AnyDesk sessions = "prenos u drugu državu" (ZZPL čl. 63 ff.)? Is AnyDesk GmbH a sub-processor? | No authoritative Serbian source; Poverenik praksa DB unfetchable (JS app, 404s) | Lawyer/Poverenik: classification of relay-server remote-desktop sessions; consequences for DPA transfer clause and čl. 23 notice | Defective DPA transfer clause; čl. 23 notice incomplete (50k–2M RSD exposure for the shop) |
| 6 | Poverenik's stance on incidental-access support without a DPA | EDPB analogy solid; no Serbian opinion verifiable | Poverenik (issues opinions on request): can occasional bug-fix sessions run under security-measures-only? | Mitigated entirely by signing the DPA (F-1) — residual risk ≈ 0 if F-1 done |
| 7 | Retention classification of POS shift/cash-movement records (statutory labour record → trajno? pomoćna knjiga → 5y? operational → čl. 5 ZZPL purge?) | Statutes don't classify POS-generated records; accountant-dependent | Shop's accountant + lawyer: documented retention rule per record type | Purge feature (SW-13) defaults could delete something an inspector later demands, or over-retain contrary to ZZPL čl. 5 |
| 8 | Certification unknowns: administrative taksa for odobrenje (none found — negative unproven); 2026 tehnički pregled queue time; current Tehnički vodič version (page renders dynamically); exact scope of "promena na elementu" for non-receipt changes | Uredba/TU silent on fees; no vendor-published durations post-2021 | PURS (budiefiskalizovan@gov.rs) + a recently approved small vendor (e.g., Eklipsa NET, 18.06.2026) | Only affects §5 planning — no current exposure |
| 9 | **HALF-CLOSED 31.07.2026.** The *legal rule* is now settled: the one-year aggregation is in the statute verbatim and binds the shop (see [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §2 Q1). What remains open is the *enforcement-practice* half — is a retailer without customer identity records expected to aggregate per-customer cash across visits, and **what ZZPL basis would justify identifying the buyer for that purpose**? | No written APML guidance found; the ZZPL half is newly identified | APML/tržišna inspekcija stručno mišljenje + a DPO on the lawful basis | Single-receipt warning (SW-11a) under-covers; **building a customer-identity store without a lawful basis creates its own ZZPL exposure** (preduzetnik 20.000–500.000, čl. 95 st. 1 t. 1). Ship the buyer tag as optional; document the limitation to the owner |
| 10 | Composite ID kupca format for public-funds buyers (ordering of PIB/JBKJS; prefix 12 vs separate 20) | Secondary sources disagree; PU šifarnik page dynamic | Verify against PU šifarnik + target ESIR guide at implementation | Only bites SW-19/certification — defer |
| 11 | **RESOLVED 01.08.2026 — NO obrazac is prescribed for the ZoR čl. 55 st. 6 record.** Verified four ways: (i) čl. 55 st. 6 and čl. 122 contain **no delegation clause**, so no bylaw can exist under them; (ii) the only executed delegation in this field (ZEOR čl. 44–45 → Odluka, Sl. list SRJ 40/97) prescribes only obrasci **E-1…E-7**, all outward filings to the employment service, and its employer-side tač. 3)–4) have ceased to be in force; (iii) ZoR čl. 277's transitional savings list names none; (iv) the Ministry's own bylaw list has none. **"Pravilnik 120/2014" does not exist in Serbian law** — one blog, no gazette entry; strike it wherever it appears. Contrast proving the point: where ZoR wants prescribed content it says so (čl. 121 st. 5 → Pravilnik 90/2014), and the BZR pravilnik (5/2025 …) *does* print obrasci 1–11, none of them a working-time record. **Caveat: free-form applies to the ZoR record only** — ZEOR čl. 44 + the Jedinstveni kodeks šifara DO prescribe methodology and codes for the wage record (see row 15). See [SW14-VERIFIED-RULES.md](SW14-VERIFIED-RULES.md) §2 | Settled from primary text | — | Closed |
| 12 | Pending bylaws to watch: new KEP pravilnik under amended ZoT čl. 30 (none as of 16.07.2026); jedinstveni šifarnik robe management rules (ZoT čl. 34 st. 9–10); e-otkupno mesto registry; **the ZZP čl. 6 st. 7 cenovnik bylaw (*„uslovi, sadržaj i način objavljivanja“*) — the one SW-12 is waiting on** | Fresh ministerial mandates from 35/2026, nothing published. The cenovnik bylaw was **verified absent across all 36 Sl. glasnik RS issues from 36/2026 (24.04.2026) through 71/2026 (31.07.2026)** and on must.gov.rs; čl. 217 preserves non-contrary prior bylaws but none prescribes a ZZP cenovnik standard | **Monitoring trigger (SW-12 req. 17): search Sl. glasnik RS quarterly for *cenovnik* and *zaštita potrošača* from now to the čl. 216 deadline of 01.05.2027**, alongside the standing mtt.gov.rs check | KEP module (SW-9) and product-code schema may need updates; design allowances already specified. **For SW-12 the consequence is narrower than it looks:** čl. 6 st. 2/3/4/5 are self-executing and are what SW-12 implements, so only **čl. 6 st. 6** — the open-data-portal upload, expressly keyed to *„standardom koji je propisan podzakonskim aktom“* — currently has no benchmark to hit. The shipped column set is the live **data.gov.rs practice, not law**; when the bylaw lands, swapping the mapping means editing the constant `cenovnik::COLUMNS` — a code change and a release we ship, not a setting the shop can change, and not a compliance event |
| 13 | Field-inspection treatment of companion software (do inspectors examine non-fiscal computers; how do they distinguish lawful ERP from a 175b tool?) | Only mystery-purchase (provokativna kupovina) practice is documented | Probe via the pilot shop's accountant / local Novi Pazar practice | Unknown-unknowns at first inspection; mitigated by F-7 posture memo + SW-4 |
| 14 | ZZPL amendment watch: GDPR-realignment draft circulating since 2023 (Strategija 2023–2030, Sl. glasnik 72/2023); ZZPL 87/2018 verified unamended as of 16.07.2026 | Every citation checked against 87/2018 | Re-verify gazette status before finalizing the DPA and templates | Document rot; low near-term |
| 15 | **Textile half RESOLVED 01.08.2026 — see [REMAINING-SW-VERIFIED-RULES.md](REMAINING-SW-VERIFIED-RULES.md) §2d.** The pravilnik is Sl. glasnik RS **1/2014 i 86/2019**, in force (ZTZP 49/2021 čl. 50 keeps it alive), **but čl. 21–24 — care (SRPS EN ISO 3758), shrinkage, size and their placement — ceased to have effect on 1 January 2023** by čl. 28 as substituted by 86/2019. Only *sirovinski sastav* survives, and it binds the proizvođač/uvoznik, not a reselling boutique. **SW-20 is descoped.** One fact question remains for the shop, not a lawyer: **does the pilot import directly on its own name?** If so it is an *uvoznik* (čl. 15 st. 2) and the whole answer flips. Still not verified: ZZP26 nesaobraznost changes (1-year presumption, čl. 56/59); prescribed form of potvrda o prijemu reklamacije. **RESOLVED 17.07.2026:** the ZoT čl. 36–37 seasonal-window and kaznene-odredbe items are now verified — see [ZOT-36-37-VERIFIED-RULES.md](ZOT-36-37-VERIFIED-RULES.md) §1 and §4 (penalty moved čl. 68→čl. 67 and **dropped from 500k–2M to a fixed 100k/40k/10k**). That memo's own §6 carries 10 residual questions, the sharpest being the <15-day-in-assortment case and whether „asortiman“ covers returning seasonal goods | Flagged in §2 row 34 | Validate at feature-implementation time (SW-7, SW-20) | Low — feature-level details, not posture |
| 16 | **Does a missing mašinski čitljiva oznaka (ZoT čl. 34 st. 8) count as „neuredna deklaracija“ (čl. 67, fixed 40k) or „roba bez deklaracije“ (čl. 68, 50k–500k + 6mo–2yr trading ban)?** | Neither penalty tačka names st. 8; both cross-refer generically to "(član 34)". No prekršajni-sud practice located | Lawyer / tržišna inspekcija | **High** — the gap between the two tiers is 40.000 vs 500.000 **plus possible shop closure**. Attach no fine figure to a bare missing GTIN until answered |
| 17 | **Is a preduzetnik a „fizičko lice“ for the ZAG čl. 9 st. 2 carve-out, and does čl. 65 st. 1 tač. 6 independently reach a private pravno lice when čl. 16 st. 2 is textually public-sector?** | ZAG čl. 2 t. 3 lists „preduzetnika“ separately from „fizičkih lica“; čl. 9 st. 1 and st. 4 carry no carve-out; čl. 65 st. 1 tač. 6 was never quoted verbatim in any pass | Lawyer / nadležni arhiv (Novi Pazar) | Medium — drives whether SW-3's reset warning states a real duty or a false one, for both shop profiles |
| 18 | **Full residual set for SW-11/SW-15: 17 open questions in [SW11-SW15-VERIFIED-RULES.md](SW11-SW15-VERIFIED-RULES.md) §5**, incl. the čl. 46 conversion rate/date (Q-4), whether Saturday is a „radni dan“ (Q-5), whether till-funded expenses reduce the polog duty (Q-6), and the ZoRač čl. 28 st. 12 question for any future hosted archive (Q-16) | Verified 31.07.2026 | Per-question, see that memo | Tracked there to keep this register readable |

---

**A note on how to read the fine figures in this document (added 31.07.2026):** every amount is now tagged with its subject tier. The pilot shop is a **preduzetnik**, so the *pravno lice* figures — which are what most secondary sources quote — do **not** apply to it, and the *privredni prestup* category is unavailable against it entirely. In-app legal copy must resolve its tier from the SW-15 `pravna_forma` field, never from a hard-coded constant.

**Bottom line for the founder:** the pilot as currently built and postured is lawful today. The five P0 items (banner, backup encryption, retention-guarded reset, anti-evasion audit, ESIR nudge) close every gap that carries vendor-side criminal or prekršaj exposure. The shop's own biggest uncovered duties an inspector would check first were the reklamacije register, KEP, and prethodna cena. **The reklamacije register is covered as of 31.07.2026 (SW-7)** — with the two residuals named in rows 22 and 23; KEP and prethodna cena remain on the P1 software roadmap. Nothing in the handoff question needs to be resolved before the double-ring pilot; it must be resolved in writing (F-5) before any ESIR integration ships.